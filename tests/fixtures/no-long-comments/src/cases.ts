// This comment runs one character past the accepted limit because the filler keeps growing and the e
export const first = 1

export const second = 2 // The trailing explanation also stretches far beyond the accepted limit because it never stops and t

// This block of prose keeps explaining and explaining because nobody stopped it from restating the entire history of the module and the explanation k
export const third = 3

export const templated = `${1}.${2}`
// This overlong comment after a template substitution must still be observed by the scanner because it exceeds the cap.
export const afterTemplate = 10
