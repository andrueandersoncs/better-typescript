// This comment lands exactly on the accepted limit because the filler stops right at the boundary a
export const within = 1

// Short comment stays within bounds.
export const short = 2

export const templatedAllowed = `${1}.${2}`
// Comment within the cap after a template because rescanning keeps the scanner in sync.
export const afterTemplateAllowed = 6
