export {}

const plansByProgram = new WeakMap<object, ReadonlyArray<string>>()

const declaredCache: WeakMap<object, number> | undefined = undefined

{
  const localCache = new WeakMap<object, boolean>()

  void localCache
}

type BuiltInCache = WeakMap<object, Date>

const builtInConstructor = WeakMap

void plansByProgram
void declaredCache
void builtInConstructor
