import * as path from "node:path"
import { testDirectory } from "./architectureEvidenceReuseTestDirectory.js"

export const importUsageFixturePath = path.join(
  testDirectory,
  "fixtures",
  "architecture-evidence-import-usage"
)
