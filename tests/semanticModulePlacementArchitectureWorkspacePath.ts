import * as path from "node:path"
import { fixturesRoot } from "./semanticModulePlacementFixturesRoot.js"

export const architectureWorkspacePath = path.join(fixturesRoot, "architecture-evidence-workspace")
