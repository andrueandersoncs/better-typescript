# DOC-14 source fidelity — third rerun

**Status: FAIL**

- Catalog rows: 131.
- Unique catalog rows: 131.
- Missing rows: 0.
- Duplicate rows: 0.
- Failed pages: 81.
- Failed judgments: 91 (`What it does`: 81; `When to use it`: 10; Conformant: 0; Non-conformant: 0).
- Passing judgments: 433 of 524.
- Catalog duplicates: 0. Missing candidate pages: 0. Extra candidate pages: 0.
- Missing rule packages: 0. Missing production source sets: 0. Missing test sets: 0. Missing fixture sets: 0.
- Reviewed files: 131 pages, 131 production Go files, 131 Go test files, and 374 fixture files (369 under `testdata/` plus 5 additional relevant files under `schema_name_suffix/testdata_v4/`).
- Result: DOC-14 does not pass the zero-failure source-fidelity gate.

## Method

- Used the 131 sorted links in `docs/rules.md` as the row authority.
- Mapped each slug to `internal/rules/<slug_with_underscores>/`.
- Freshly read every page, every production `.go` file excluding `*_test.go`, every `*_test.go` file, and every file under all relevant `testdata*` directories.
- Included both `testdata/` and `testdata_v4/` for `schema-name-suffix`.
- Judged `What it does`, `When to use it`, Conformant, and Non-conformant separately.
- Checked listener forms, import and symbol resolution, textual versus semantic behavior, finite name sets, traversal limits, entry gates, certainty checks, exemptions, and false-positive boundaries.
- Split the catalog into 12 read-only catalog-order reviews, then reconciled every judgment against one inventory. Candidate inputs did not change during the review.
- Corroborating command `mise exec go@1.26 -- go test ./internal/rules/...` passed.

## Matrix

| Slug | Page | Production Go | Tests | Fixtures | What it does | When to use it | Conformant | Non-conformant |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `boundary-schema-decode` | `docs/rules/boundary-schema-decode.md` | `internal/rules/boundary_schema_decode/rule.go` | `internal/rules/boundary_schema_decode/rule_test.go` | `internal/rules/boundary_schema_decode/testdata/index.ts`<br>`internal/rules/boundary_schema_decode/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `bounded-retry-schedule` | `docs/rules/bounded-retry-schedule.md` | `internal/rules/bounded_retry_schedule/rule.go` | `internal/rules/bounded_retry_schedule/rule_test.go` | `internal/rules/bounded_retry_schedule/testdata/index.ts`<br>`internal/rules/bounded_retry_schedule/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `cache-per-request` | `docs/rules/cache-per-request.md` | `internal/rules/cache_per_request/rule.go` | `internal/rules/cache_per_request/rule_test.go` | `internal/rules/cache_per_request/testdata/index.ts`<br>`internal/rules/cache_per_request/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `cache-preference` | `docs/rules/cache-preference.md` | `internal/rules/cache_preference/rule.go` | `internal/rules/cache_preference/rule_test.go` | `internal/rules/cache_preference/testdata/index.ts`<br>`internal/rules/cache_preference/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `closed-abstraction` | `docs/rules/closed-abstraction.md` | `internal/rules/closed_abstraction/rule.go` | `internal/rules/closed_abstraction/rule_test.go` | `internal/rules/closed_abstraction/testdata/index.ts`<br>`internal/rules/closed_abstraction/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `config-refined-values` | `docs/rules/config-refined-values.md` | `internal/rules/config_refined_values/rule.go` | `internal/rules/config_refined_values/rule_test.go` | `internal/rules/config_refined_values/testdata/index.ts`<br>`internal/rules/config_refined_values/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `dependent-layer-merge` | `docs/rules/dependent-layer-merge.md` | `internal/rules/dependent_layer_merge/rule.go` | `internal/rules/dependent_layer_merge/rule_test.go` | `internal/rules/dependent_layer_merge/testdata/index.ts`<br>`internal/rules/dependent_layer_merge/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `duplicate-shape` | `docs/rules/duplicate-shape.md` | `internal/rules/duplicate_shape/rule.go` | `internal/rules/duplicate_shape/rule_test.go` | `internal/rules/duplicate_shape/testdata/index.ts`<br>`internal/rules/duplicate_shape/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `effect-fn-name` | `docs/rules/effect-fn-name.md` | `internal/rules/effect_fn_name/rule.go` | `internal/rules/effect_fn_name/rule_test.go` | `internal/rules/effect_fn_name/testdata/index.ts`<br>`internal/rules/effect_fn_name/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `effect-test-style` | `docs/rules/effect-test-style.md` | `internal/rules/effect_test_style/rule.go` | `internal/rules/effect_test_style/rule_test.go` | `internal/rules/effect_test_style/testdata/index.ts`<br>`internal/rules/effect_test_style/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `function-derived-model` | `docs/rules/function-derived-model.md` | `internal/rules/function_derived_model/rule.go` | `internal/rules/function_derived_model/rule_test.go` | `internal/rules/function_derived_model/testdata/index.ts`<br>`internal/rules/function_derived_model/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `global-config-mutation` | `docs/rules/global-config-mutation.md` | `internal/rules/global_config_mutation/rule.go` | `internal/rules/global_config_mutation/rule_test.go` | `internal/rules/global_config_mutation/testdata/index.ts`<br>`internal/rules/global_config_mutation/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `handrolled-ttl-cache` | `docs/rules/handrolled-ttl-cache.md` | `internal/rules/handrolled_ttl_cache/rule.go` | `internal/rules/handrolled_ttl_cache/rule_test.go` | `internal/rules/handrolled_ttl_cache/testdata/index.ts`<br>`internal/rules/handrolled_ttl_cache/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `http-client-preference` | `docs/rules/http-client-preference.md` | `internal/rules/http_client_preference/rule.go` | `internal/rules/http_client_preference/rule_test.go` | `internal/rules/http_client_preference/testdata/index.ts`<br>`internal/rules/http_client_preference/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `http-response-validation` | `docs/rules/http-response-validation.md` | `internal/rules/http_response_validation/rule.go` | `internal/rules/http_response_validation/rule_test.go` | `internal/rules/http_response_validation/testdata/index.ts`<br>`internal/rules/http_response_validation/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `http-status-decode-order` | `docs/rules/http-status-decode-order.md` | `internal/rules/http_status_decode_order/rule.go` | `internal/rules/http_status_decode_order/rule_test.go` | `internal/rules/http_status_decode_order/testdata/index.ts`<br>`internal/rules/http_status_decode_order/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `idempotent-retry` | `docs/rules/idempotent-retry.md` | `internal/rules/idempotent_retry/idempotent_retry.go` | `internal/rules/idempotent_retry/rule_test.go` | `internal/rules/idempotent_retry/testdata/src/clean.ts`<br>`internal/rules/idempotent_retry/testdata/src/violation.ts`<br>`internal/rules/idempotent_retry/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `inflight-dedupe-map` | `docs/rules/inflight-dedupe-map.md` | `internal/rules/inflight_dedupe_map/inflight_dedupe_map.go` | `internal/rules/inflight_dedupe_map/rule_test.go` | `internal/rules/inflight_dedupe_map/testdata/src/clean.ts`<br>`internal/rules/inflight_dedupe_map/testdata/src/violation.ts`<br>`internal/rules/inflight_dedupe_map/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `layer-forever-acquisition` | `docs/rules/layer-forever-acquisition.md` | `internal/rules/layer_forever_acquisition/layer_forever_acquisition.go` | `internal/rules/layer_forever_acquisition/rule_test.go` | `internal/rules/layer_forever_acquisition/testdata/src/clean.ts`<br>`internal/rules/layer_forever_acquisition/testdata/src/violation.ts`<br>`internal/rules/layer_forever_acquisition/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `missing-rationale` | `docs/rules/missing-rationale.md` | `internal/rules/missing_rationale/missing_rationale.go` | `internal/rules/missing_rationale/rule_test.go` | `internal/rules/missing_rationale/testdata/src/clean.ts`<br>`internal/rules/missing_rationale/testdata/src/violation.ts`<br>`internal/rules/missing_rationale/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-async-functions` | `docs/rules/no-async-functions.md` | `internal/rules/no_async_functions/no_async_functions.go` | `internal/rules/no_async_functions/rule_test.go` | `internal/rules/no_async_functions/testdata/src/clean.ts`<br>`internal/rules/no_async_functions/testdata/src/violation.ts`<br>`internal/rules/no_async_functions/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-blank-lines-between-single-line-declarations` | `docs/rules/no-blank-lines-between-single-line-declarations.md` | `internal/rules/no_blank_lines_between_single_line_declarations/no_blank_lines_between_single_line_declarations.go` | `internal/rules/no_blank_lines_between_single_line_declarations/rule_test.go` | `internal/rules/no_blank_lines_between_single_line_declarations/testdata/src/clean.ts`<br>`internal/rules/no_blank_lines_between_single_line_declarations/testdata/src/violation.ts`<br>`internal/rules/no_blank_lines_between_single_line_declarations/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-callbacks` | `docs/rules/no-callbacks.md` | `internal/rules/no_callbacks/no_callbacks.go` | `internal/rules/no_callbacks/rule_test.go` | `internal/rules/no_callbacks/testdata/src/clean.ts`<br>`internal/rules/no_callbacks/testdata/src/violation.ts`<br>`internal/rules/no_callbacks/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-duplicate-function-names` | `docs/rules/no-duplicate-function-names.md` | `internal/rules/no_duplicate_function_names/no_duplicate_function_names.go` | `internal/rules/no_duplicate_function_names/rule_test.go` | `internal/rules/no_duplicate_function_names/testdata/src/clean.ts`<br>`internal/rules/no_duplicate_function_names/testdata/src/duplicate.ts`<br>`internal/rules/no_duplicate_function_names/testdata/src/violation.ts`<br>`internal/rules/no_duplicate_function_names/testdata/tsconfig.json` | **FAIL** | **FAIL** | PASS | PASS |
| `no-duplicate-if-bodies` | `docs/rules/no-duplicate-if-bodies.md` | `internal/rules/no_duplicate_if_bodies/no_duplicate_if_bodies.go` | `internal/rules/no_duplicate_if_bodies/rule_test.go` | `internal/rules/no_duplicate_if_bodies/testdata/src/clean.ts`<br>`internal/rules/no_duplicate_if_bodies/testdata/src/violation.ts`<br>`internal/rules/no_duplicate_if_bodies/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-error-type` | `docs/rules/no-error-type.md` | `internal/rules/no_error_type/no_error_type.go` | `internal/rules/no_error_type/rule_test.go` | `internal/rules/no_error_type/testdata/src/clean.ts`<br>`internal/rules/no_error_type/testdata/src/violation.ts`<br>`internal/rules/no_error_type/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-explicit-any-return` | `docs/rules/no-explicit-any-return.md` | `internal/rules/no_explicit_any_return/no_explicit_any_return.go` | `internal/rules/no_explicit_any_return/rule_test.go` | `internal/rules/no_explicit_any_return/testdata/src/clean.ts`<br>`internal/rules/no_explicit_any_return/testdata/src/violation.ts`<br>`internal/rules/no_explicit_any_return/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-first-party-schema-declare` | `docs/rules/no-first-party-schema-declare.md` | `internal/rules/no_first_party_schema_declare/no_first_party_schema_declare.go` | `internal/rules/no_first_party_schema_declare/rule_test.go` | `internal/rules/no_first_party_schema_declare/testdata/src/clean.ts`<br>`internal/rules/no_first_party_schema_declare/testdata/src/violation.ts`<br>`internal/rules/no_first_party_schema_declare/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-for-in-loops` | `docs/rules/no-for-in-loops.md` | `internal/rules/no_for_in_loops/no_for_in_loops.go` | `internal/rules/no_for_in_loops/rule_test.go` | `internal/rules/no_for_in_loops/testdata/src/clean.ts`<br>`internal/rules/no_for_in_loops/testdata/src/violation.ts`<br>`internal/rules/no_for_in_loops/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-for-loops` | `docs/rules/no-for-loops.md` | `internal/rules/no_for_loops/no_for_loops.go` | `internal/rules/no_for_loops/rule_test.go` | `internal/rules/no_for_loops/testdata/src/clean.ts`<br>`internal/rules/no_for_loops/testdata/src/violation.ts`<br>`internal/rules/no_for_loops/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-for-of-loops` | `docs/rules/no-for-of-loops.md` | `internal/rules/no_for_of_loops/no_for_of_loops.go` | `internal/rules/no_for_of_loops/rule_test.go` | `internal/rules/no_for_of_loops/testdata/src/clean.ts`<br>`internal/rules/no_for_of_loops/testdata/src/violation.ts`<br>`internal/rules/no_for_of_loops/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-function-keyword` | `docs/rules/no-function-keyword.md` | `internal/rules/no_function_keyword/no_function_keyword.go` | `internal/rules/no_function_keyword/rule_test.go` | `internal/rules/no_function_keyword/testdata/src/clean.ts`<br>`internal/rules/no_function_keyword/testdata/src/violation.ts`<br>`internal/rules/no_function_keyword/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-immediate-effect-sync` | `docs/rules/no-immediate-effect-sync.md` | `internal/rules/no_immediate_effect_sync/no_immediate_effect_sync.go` | `internal/rules/no_immediate_effect_sync/rule_test.go` | `internal/rules/no_immediate_effect_sync/testdata/project/src/cases.ts`<br>`internal/rules/no_immediate_effect_sync/testdata/project/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-inline-boolean-expressions` | `docs/rules/no-inline-boolean-expressions.md` | `internal/rules/no_inline_boolean_expressions/no_inline_boolean_expressions.go` | `internal/rules/no_inline_boolean_expressions/rule_test.go` | `internal/rules/no_inline_boolean_expressions/testdata/project/src/cases.ts`<br>`internal/rules/no_inline_boolean_expressions/testdata/project/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-inline-closures` | `docs/rules/no-inline-closures.md` | `internal/rules/no_inline_closures/no_inline_closures.go` | `internal/rules/no_inline_closures/rule_test.go` | `internal/rules/no_inline_closures/testdata/project/src/cases.ts`<br>`internal/rules/no_inline_closures/testdata/project/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-instanceof` | `docs/rules/no-instanceof.md` | `internal/rules/no_instanceof/no_instanceof.go` | `internal/rules/no_instanceof/rule_test.go` | `internal/rules/no_instanceof/testdata/project/src/cases.ts`<br>`internal/rules/no_instanceof/testdata/project/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-manual-type-dispatch` | `docs/rules/no-manual-type-dispatch.md` | `internal/rules/no_manual_type_dispatch/no_manual_type_dispatch.go` | `internal/rules/no_manual_type_dispatch/rule_test.go` | `internal/rules/no_manual_type_dispatch/testdata/project/src/cases.ts`<br>`internal/rules/no_manual_type_dispatch/testdata/project/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-monomorphic-struct-get` | `docs/rules/no-monomorphic-struct-get.md` | `internal/rules/no_monomorphic_struct_get/no_monomorphic_struct_get.go` | `internal/rules/no_monomorphic_struct_get/rule_test.go` | `internal/rules/no_monomorphic_struct_get/testdata/project/node_modules/effect/Struct.d.ts`<br>`internal/rules/no_monomorphic_struct_get/testdata/project/node_modules/effect/index.d.ts`<br>`internal/rules/no_monomorphic_struct_get/testdata/project/node_modules/effect/package.json`<br>`internal/rules/no_monomorphic_struct_get/testdata/project/src/cases.ts`<br>`internal/rules/no_monomorphic_struct_get/testdata/project/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-multiple-boolean-operators` | `docs/rules/no-multiple-boolean-operators.md` | `internal/rules/no_multiple_boolean_operators/no_multiple_boolean_operators.go` | `internal/rules/no_multiple_boolean_operators/rule_test.go` | `internal/rules/no_multiple_boolean_operators/testdata/project/src/cases.ts`<br>`internal/rules/no_multiple_boolean_operators/testdata/project/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-mutable-array-methods` | `docs/rules/no-mutable-array-methods.md` | `internal/rules/no_mutable_array_methods/no_mutable_array_methods.go` | `internal/rules/no_mutable_array_methods/rule_test.go` | `internal/rules/no_mutable_array_methods/testdata/project/src/cases.ts`<br>`internal/rules/no_mutable_array_methods/testdata/project/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-mutable-variable-declarations` | `docs/rules/no-mutable-variable-declarations.md` | `internal/rules/no_mutable_variable_declarations/no_mutable_variable_declarations.go` | `internal/rules/no_mutable_variable_declarations/rule_test.go` | `internal/rules/no_mutable_variable_declarations/testdata/project/src/cases.ts`<br>`internal/rules/no_mutable_variable_declarations/testdata/project/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-mutation` | `docs/rules/no-mutation.md` | `internal/rules/no_mutation/no_mutation.go` | `internal/rules/no_mutation/rule_test.go` | `internal/rules/no_mutation/testdata/project/src/cases.ts`<br>`internal/rules/no_mutation/testdata/project/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-nested-calls` | `docs/rules/no-nested-calls.md` | `internal/rules/no_nested_calls/no_nested_calls.go` | `internal/rules/no_nested_calls/rule_test.go` | `internal/rules/no_nested_calls/testdata/project/src/cases.ts`<br>`internal/rules/no_nested_calls/testdata/project/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-nested-if-statements` | `docs/rules/no-nested-if-statements.md` | `internal/rules/no_nested_if_statements/no_nested_if_statements.go` | `internal/rules/no_nested_if_statements/rule_test.go` | `internal/rules/no_nested_if_statements/testdata/project/src/cases.ts`<br>`internal/rules/no_nested_if_statements/testdata/project/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-new-error` | `docs/rules/no-new-error.md` | `internal/rules/no_new_error/no_new_error.go` | `internal/rules/no_new_error/rule_test.go` | `internal/rules/no_new_error/testdata/project/src/cases.ts`<br>`internal/rules/no_new_error/testdata/project/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-non-null-assertion` | `docs/rules/no-non-null-assertion.md` | `internal/rules/no_non_null_assertion/no_non_null_assertion.go` | `internal/rules/no_non_null_assertion/rule_test.go` | `internal/rules/no_non_null_assertion/testdata/project/src/cases.ts`<br>`internal/rules/no_non_null_assertion/testdata/project/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-pass-through-object-wrappers` | `docs/rules/no-pass-through-object-wrappers.md` | `internal/rules/no_pass_through_object_wrappers/no_pass_through_object_wrappers.go` | `internal/rules/no_pass_through_object_wrappers/rule_test.go` | `internal/rules/no_pass_through_object_wrappers/testdata/project/src/cases.ts`<br>`internal/rules/no_pass_through_object_wrappers/testdata/project/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-raw-object-types` | `docs/rules/no-raw-object-types.md` | `internal/rules/no_raw_object_types/no_raw_object_types.go` | `internal/rules/no_raw_object_types/rule_test.go` | `internal/rules/no_raw_object_types/testdata/project/src/cases.ts`<br>`internal/rules/no_raw_object_types/testdata/project/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-reexports` | `docs/rules/no-reexports.md` | `internal/rules/no_reexports/rule.go` | `internal/rules/no_reexports/rule_test.go` | `internal/rules/no_reexports/testdata/dependency.ts`<br>`internal/rules/no_reexports/testdata/index.ts`<br>`internal/rules/no_reexports/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-switch-statements` | `docs/rules/no-switch-statements.md` | `internal/rules/no_switch_statements/rule.go` | `internal/rules/no_switch_statements/rule_test.go` | `internal/rules/no_switch_statements/testdata/index.ts`<br>`internal/rules/no_switch_statements/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-throw` | `docs/rules/no-throw.md` | `internal/rules/no_throw/rule.go` | `internal/rules/no_throw/rule_test.go` | `internal/rules/no_throw/testdata/index.ts`<br>`internal/rules/no_throw/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-trivial-effect-fn` | `docs/rules/no-trivial-effect-fn.md` | `internal/rules/no_trivial_effect_fn/rule.go` | `internal/rules/no_trivial_effect_fn/rule_test.go` | `internal/rules/no_trivial_effect_fn/testdata/effect/index.d.ts`<br>`internal/rules/no_trivial_effect_fn/testdata/index.ts`<br>`internal/rules/no_trivial_effect_fn/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-try-catch` | `docs/rules/no-try-catch.md` | `internal/rules/no_try_catch/rule.go` | `internal/rules/no_try_catch/rule_test.go` | `internal/rules/no_try_catch/testdata/index.ts`<br>`internal/rules/no_try_catch/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-undefined` | `docs/rules/no-undefined.md` | `internal/rules/no_undefined/rule.go` | `internal/rules/no_undefined/rule_test.go` | `internal/rules/no_undefined/testdata/index.ts`<br>`internal/rules/no_undefined/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-unsafe-effect-apis` | `docs/rules/no-unsafe-effect-apis.md` | `internal/rules/no_unsafe_effect_apis/rule.go` | `internal/rules/no_unsafe_effect_apis/rule_test.go` | `internal/rules/no_unsafe_effect_apis/testdata/effect/index.d.ts`<br>`internal/rules/no_unsafe_effect_apis/testdata/index.ts`<br>`internal/rules/no_unsafe_effect_apis/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-unused` | `docs/rules/no-unused.md` | `internal/rules/no_unused/rule.go` | `internal/rules/no_unused/rule_test.go` | `internal/rules/no_unused/testdata/ignored.ts`<br>`internal/rules/no_unused/testdata/index.ts`<br>`internal/rules/no_unused/testdata/second.ts`<br>`internal/rules/no_unused/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-value-aliases` | `docs/rules/no-value-aliases.md` | `internal/rules/no_value_aliases/rule.go` | `internal/rules/no_value_aliases/rule_test.go` | `internal/rules/no_value_aliases/testdata/index.ts`<br>`internal/rules/no_value_aliases/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `no-void-functions` | `docs/rules/no-void-functions.md` | `internal/rules/no_void_functions/rule.go` | `internal/rules/no_void_functions/rule_test.go` | `internal/rules/no_void_functions/testdata/index.ts`<br>`internal/rules/no_void_functions/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `no-weak-map` | `docs/rules/no-weak-map.md` | `internal/rules/no_weak_map/rule.go` | `internal/rules/no_weak_map/rule_test.go` | `internal/rules/no_weak_map/testdata/index.ts`<br>`internal/rules/no_weak_map/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `observable-worker-failure` | `docs/rules/observable-worker-failure.md` | `internal/rules/observable_worker_failure/rule.go` | `internal/rules/observable_worker_failure/rule_test.go` | `internal/rules/observable_worker_failure/testdata/effect/index.d.ts`<br>`internal/rules/observable_worker_failure/testdata/index.ts`<br>`internal/rules/observable_worker_failure/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `parameter-bag` | `docs/rules/parameter-bag.md` | `internal/rules/parameter_bag/rule.go` | `internal/rules/parameter_bag/rule_test.go` | `internal/rules/parameter_bag/testdata/anonymous.ts`<br>`internal/rules/parameter_bag/testdata/consumer.ts`<br>`internal/rules/parameter_bag/testdata/index.ts`<br>`internal/rules/parameter_bag/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `pass-through-conversion` | `docs/rules/pass-through-conversion.md` | `internal/rules/pass_through_conversion/rule.go` | `internal/rules/pass_through_conversion/rule_test.go` | `internal/rules/pass_through_conversion/testdata/index.ts`<br>`internal/rules/pass_through_conversion/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `prefer-composed-callbacks` | `docs/rules/prefer-composed-callbacks.md` | `internal/rules/prefer_composed_callbacks/rule.go` | `internal/rules/prefer_composed_callbacks/rule_test.go` | `internal/rules/prefer_composed_callbacks/testdata/index.ts`<br>`internal/rules/prefer_composed_callbacks/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `prefer-conditional-return` | `docs/rules/prefer-conditional-return.md` | `internal/rules/prefer_conditional_return/rule.go` | `internal/rules/prefer_conditional_return/rule_test.go` | `internal/rules/prefer_conditional_return/testdata/index.ts`<br>`internal/rules/prefer_conditional_return/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `prefer-context-service-class` | `docs/rules/prefer-context-service-class.md` | `internal/rules/prefer_context_service_class/rule.go` | `internal/rules/prefer_context_service_class/rule_test.go` | `internal/rules/prefer_context_service_class/testdata/clean.ts`<br>`internal/rules/prefer_context_service_class/testdata/node_modules/effect/index.d.ts`<br>`internal/rules/prefer_context_service_class/testdata/node_modules/effect/package.json`<br>`internal/rules/prefer_context_service_class/testdata/tsconfig.json`<br>`internal/rules/prefer_context_service_class/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-curried-data-last-functions` | `docs/rules/prefer-curried-data-last-functions.md` | `internal/rules/prefer_curried_data_last_functions/rule.go` | `internal/rules/prefer_curried_data_last_functions/rule_test.go` | `internal/rules/prefer_curried_data_last_functions/testdata/clean.ts`<br>`internal/rules/prefer_curried_data_last_functions/testdata/tsconfig.json`<br>`internal/rules/prefer_curried_data_last_functions/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `prefer-direct-boolean-return` | `docs/rules/prefer-direct-boolean-return.md` | `internal/rules/prefer_direct_boolean_return/rule.go` | `internal/rules/prefer_direct_boolean_return/rule_test.go` | `internal/rules/prefer_direct_boolean_return/testdata/clean.ts`<br>`internal/rules/prefer_direct_boolean_return/testdata/tsconfig.json`<br>`internal/rules/prefer_direct_boolean_return/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-direct-yield` | `docs/rules/prefer-direct-yield.md` | `internal/rules/prefer_direct_yield/rule.go` | `internal/rules/prefer_direct_yield/rule_test.go` | `internal/rules/prefer_direct_yield/testdata/clean.ts`<br>`internal/rules/prefer_direct_yield/testdata/node_modules/effect/index.d.ts`<br>`internal/rules/prefer_direct_yield/testdata/node_modules/effect/package.json`<br>`internal/rules/prefer_direct_yield/testdata/tsconfig.json`<br>`internal/rules/prefer_direct_yield/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-effect-array` | `docs/rules/prefer-effect-array.md` | `internal/rules/prefer_effect_array/rule.go` | `internal/rules/prefer_effect_array/rule_test.go` | `internal/rules/prefer_effect_array/testdata/clean.ts`<br>`internal/rules/prefer_effect_array/testdata/tsconfig.json`<br>`internal/rules/prefer_effect_array/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-effect-array-append-all` | `docs/rules/prefer-effect-array-append-all.md` | `internal/rules/prefer_effect_array_append_all/rule.go` | `internal/rules/prefer_effect_array_append_all/rule_test.go` | `internal/rules/prefer_effect_array_append_all/testdata/clean.ts`<br>`internal/rules/prefer_effect_array_append_all/testdata/tsconfig.json`<br>`internal/rules/prefer_effect_array_append_all/testdata/violation.ts` | **FAIL** | **FAIL** | PASS | PASS |
| `prefer-effect-array-count-by` | `docs/rules/prefer-effect-array-count-by.md` | `internal/rules/prefer_effect_array_count_by/rule.go` | `internal/rules/prefer_effect_array_count_by/rule_test.go` | `internal/rules/prefer_effect_array_count_by/testdata/clean.ts`<br>`internal/rules/prefer_effect_array_count_by/testdata/node_modules/effect/Array.d.ts`<br>`internal/rules/prefer_effect_array_count_by/testdata/node_modules/effect/package.json`<br>`internal/rules/prefer_effect_array_count_by/testdata/tsconfig.json`<br>`internal/rules/prefer_effect_array_count_by/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-effect-fn` | `docs/rules/prefer-effect-fn.md` | `internal/rules/prefer_effect_fn/rule.go` | `internal/rules/prefer_effect_fn/rule_test.go` | `internal/rules/prefer_effect_fn/testdata/clean.ts`<br>`internal/rules/prefer_effect_fn/testdata/node_modules/effect/index.d.ts`<br>`internal/rules/prefer_effect_fn/testdata/node_modules/effect/package.json`<br>`internal/rules/prefer_effect_fn/testdata/tsconfig.json`<br>`internal/rules/prefer_effect_fn/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-effect-function-constant` | `docs/rules/prefer-effect-function-constant.md` | `internal/rules/prefer_effect_function_constant/rule.go` | `internal/rules/prefer_effect_function_constant/rule_test.go` | `internal/rules/prefer_effect_function_constant/testdata/clean.ts`<br>`internal/rules/prefer_effect_function_constant/testdata/tsconfig.json`<br>`internal/rules/prefer_effect_function_constant/testdata/violation.ts` | **FAIL** | **FAIL** | PASS | PASS |
| `prefer-effect-index-access` | `docs/rules/prefer-effect-index-access.md` | `internal/rules/prefer_effect_index_access/rule.go` | `internal/rules/prefer_effect_index_access/rule_test.go` | `internal/rules/prefer_effect_index_access/testdata/clean.ts`<br>`internal/rules/prefer_effect_index_access/testdata/tsconfig.json`<br>`internal/rules/prefer_effect_index_access/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-effect-property-accessors` | `docs/rules/prefer-effect-property-accessors.md` | `internal/rules/prefer_effect_property_accessors/rule.go` | `internal/rules/prefer_effect_property_accessors/rule_test.go` | `internal/rules/prefer_effect_property_accessors/testdata/clean.ts`<br>`internal/rules/prefer_effect_property_accessors/testdata/tsconfig.json`<br>`internal/rules/prefer_effect_property_accessors/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-effect-record-filter-map` | `docs/rules/prefer-effect-record-filter-map.md` | `internal/rules/prefer_effect_record_filter_map/rule.go` | `internal/rules/prefer_effect_record_filter_map/rule_test.go` | `internal/rules/prefer_effect_record_filter_map/testdata/clean.ts`<br>`internal/rules/prefer_effect_record_filter_map/testdata/tsconfig.json`<br>`internal/rules/prefer_effect_record_filter_map/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-effect-schema-constructor` | `docs/rules/prefer-effect-schema-constructor.md` | `internal/rules/prefer_effect_schema_constructor/rule.go` | `internal/rules/prefer_effect_schema_constructor/rule_test.go` | `internal/rules/prefer_effect_schema_constructor/testdata/clean.ts`<br>`internal/rules/prefer_effect_schema_constructor/testdata/tsconfig.json`<br>`internal/rules/prefer_effect_schema_constructor/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `prefer-effect-schema-guard` | `docs/rules/prefer-effect-schema-guard.md` | `internal/rules/prefer_effect_schema_guard/rule.go` | `internal/rules/prefer_effect_schema_guard/rule_test.go` | `internal/rules/prefer_effect_schema_guard/testdata/clean.ts`<br>`internal/rules/prefer_effect_schema_guard/testdata/tsconfig.json`<br>`internal/rules/prefer_effect_schema_guard/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `prefer-effect-schema-is` | `docs/rules/prefer-effect-schema-is.md` | `internal/rules/prefer_effect_schema_is/rule.go` | `internal/rules/prefer_effect_schema_is/rule_test.go` | `internal/rules/prefer_effect_schema_is/testdata/clean.ts`<br>`internal/rules/prefer_effect_schema_is/testdata/tsconfig.json`<br>`internal/rules/prefer_effect_schema_is/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-effect-schema-record` | `docs/rules/prefer-effect-schema-record.md` | `internal/rules/prefer_effect_schema_record/rule.go` | `internal/rules/prefer_effect_schema_record/rule_test.go` | `internal/rules/prefer_effect_schema_record/testdata/clean.ts`<br>`internal/rules/prefer_effect_schema_record/testdata/tsconfig.json`<br>`internal/rules/prefer_effect_schema_record/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `prefer-effectful-function` | `docs/rules/prefer-effectful-function.md` | `internal/rules/prefer_effectful_function/rule.go` | `internal/rules/prefer_effectful_function/rule_test.go` | `internal/rules/prefer_effectful_function/testdata/clean.ts`<br>`internal/rules/prefer_effectful_function/testdata/node_modules/effect/index.d.ts`<br>`internal/rules/prefer_effectful_function/testdata/node_modules/effect/package.json`<br>`internal/rules/prefer_effectful_function/testdata/tsconfig.json`<br>`internal/rules/prefer_effectful_function/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-equivalence-strict-equal` | `docs/rules/prefer-equivalence-strict-equal.md` | `internal/rules/prefer_equivalence_strict_equal/rule.go` | `internal/rules/prefer_equivalence_strict_equal/rule_test.go` | `internal/rules/prefer_equivalence_strict_equal/testdata/clean.ts`<br>`internal/rules/prefer_equivalence_strict_equal/testdata/tsconfig.json`<br>`internal/rules/prefer_equivalence_strict_equal/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-eta-reduction` | `docs/rules/prefer-eta-reduction.md` | `internal/rules/prefer_eta_reduction/rule.go` | `internal/rules/prefer_eta_reduction/rule_test.go` | `internal/rules/prefer_eta_reduction/testdata/clean.ts`<br>`internal/rules/prefer_eta_reduction/testdata/tsconfig.json`<br>`internal/rules/prefer_eta_reduction/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `prefer-function-composition` | `docs/rules/prefer-function-composition.md` | `internal/rules/prefer_function_composition/rule.go` | `internal/rules/prefer_function_composition/rule_test.go` | `internal/rules/prefer_function_composition/testdata/clean.ts`<br>`internal/rules/prefer_function_composition/testdata/tsconfig.json`<br>`internal/rules/prefer_function_composition/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `prefer-function-flip` | `docs/rules/prefer-function-flip.md` | `internal/rules/prefer_function_flip/rule.go` | `internal/rules/prefer_function_flip/rule_test.go` | `internal/rules/prefer_function_flip/testdata/clean.ts`<br>`internal/rules/prefer_function_flip/testdata/tsconfig.json`<br>`internal/rules/prefer_function_flip/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `prefer-hash-map` | `docs/rules/prefer-hash-map.md` | `internal/rules/prefer_hash_map/rule.go` | `internal/rules/prefer_hash_map/rule_test.go` | `internal/rules/prefer_hash_map/testdata/clean.ts`<br>`internal/rules/prefer_hash_map/testdata/tsconfig.json`<br>`internal/rules/prefer_hash_map/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `prefer-hash-set` | `docs/rules/prefer-hash-set.md` | `internal/rules/prefer_hash_set/rule.go` | `internal/rules/prefer_hash_set/rule_test.go` | `internal/rules/prefer_hash_set/testdata/clean.ts`<br>`internal/rules/prefer_hash_set/testdata/tsconfig.json`<br>`internal/rules/prefer_hash_set/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `prefer-implicit-return` | `docs/rules/prefer-implicit-return.md` | `internal/rules/prefer_implicit_return/rule.go` | `internal/rules/prefer_implicit_return/rule_test.go` | `internal/rules/prefer_implicit_return/testdata/clean.ts`<br>`internal/rules/prefer_implicit_return/testdata/tsconfig.json`<br>`internal/rules/prefer_implicit_return/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-inferred-types` | `docs/rules/prefer-inferred-types.md` | `internal/rules/prefer_inferred_types/rule.go` | `internal/rules/prefer_inferred_types/rule_test.go` | `internal/rules/prefer_inferred_types/testdata/clean.ts`<br>`internal/rules/prefer_inferred_types/testdata/tsconfig.json`<br>`internal/rules/prefer_inferred_types/testdata/violation.ts` | **FAIL** | **FAIL** | PASS | PASS |
| `prefer-layer-unwrap` | `docs/rules/prefer-layer-unwrap.md` | `internal/rules/prefer_layer_unwrap/rule.go` | `internal/rules/prefer_layer_unwrap/rule_test.go` | `internal/rules/prefer_layer_unwrap/testdata/clean.ts`<br>`internal/rules/prefer_layer_unwrap/testdata/node_modules/effect/index.d.ts`<br>`internal/rules/prefer_layer_unwrap/testdata/node_modules/effect/package.json`<br>`internal/rules/prefer_layer_unwrap/testdata/tsconfig.json`<br>`internal/rules/prefer_layer_unwrap/testdata/violation.ts` | **FAIL** | **FAIL** | PASS | PASS |
| `prefer-option-match` | `docs/rules/prefer-option-match.md` | `internal/rules/prefer_option_match/rule.go` | `internal/rules/prefer_option_match/rule_test.go` | `internal/rules/prefer_option_match/testdata/clean.ts`<br>`internal/rules/prefer_option_match/testdata/tsconfig.json`<br>`internal/rules/prefer_option_match/testdata/violation.ts` | **FAIL** | **FAIL** | PASS | PASS |
| `prefer-pipe-function` | `docs/rules/prefer-pipe-function.md` | `internal/rules/prefer_pipe_function/rule.go` | `internal/rules/prefer_pipe_function/rule_test.go` | `internal/rules/prefer_pipe_function/testdata/clean.ts`<br>`internal/rules/prefer_pipe_function/testdata/node_modules/effect/index.d.ts`<br>`internal/rules/prefer_pipe_function/testdata/node_modules/effect/package.json`<br>`internal/rules/prefer_pipe_function/testdata/tsconfig.json`<br>`internal/rules/prefer_pipe_function/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `prefer-result-concept-names` | `docs/rules/prefer-result-concept-names.md` | `internal/rules/prefer_result_concept_names/rule.go` | `internal/rules/prefer_result_concept_names/rule_test.go` | `internal/rules/prefer_result_concept_names/testdata/clean.ts`<br>`internal/rules/prefer_result_concept_names/testdata/tsconfig.json`<br>`internal/rules/prefer_result_concept_names/testdata/violation.ts` | **FAIL** | **FAIL** | PASS | PASS |
| `prefer-schema-tagged-struct` | `docs/rules/prefer-schema-tagged-struct.md` | `internal/rules/prefer_schema_tagged_struct/rule.go` | `internal/rules/prefer_schema_tagged_struct/rule_test.go` | `internal/rules/prefer_schema_tagged_struct/testdata/clean.ts`<br>`internal/rules/prefer_schema_tagged_struct/testdata/node_modules/effect/index.d.ts`<br>`internal/rules/prefer_schema_tagged_struct/testdata/node_modules/effect/package.json`<br>`internal/rules/prefer_schema_tagged_struct/testdata/tsconfig.json`<br>`internal/rules/prefer_schema_tagged_struct/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `prefer-specific-operation-names` | `docs/rules/prefer-specific-operation-names.md` | `internal/rules/prefer_specific_operation_names/rule.go` | `internal/rules/prefer_specific_operation_names/rule_test.go` | `internal/rules/prefer_specific_operation_names/testdata/clean.ts`<br>`internal/rules/prefer_specific_operation_names/testdata/tsconfig.json`<br>`internal/rules/prefer_specific_operation_names/testdata/violation.ts` | **FAIL** | **FAIL** | PASS | PASS |
| `process-environment` | `docs/rules/process-environment.md` | `internal/rules/process_environment/rule.go` | `internal/rules/process_environment/rule_test.go` | `internal/rules/process_environment/testdata/clean.ts`<br>`internal/rules/process_environment/testdata/tsconfig.json`<br>`internal/rules/process_environment/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `production-sleep-loops` | `docs/rules/production-sleep-loops.md` | `internal/rules/production_sleep_loops/rule.go` | `internal/rules/production_sleep_loops/rule_test.go` | `internal/rules/production_sleep_loops/testdata/index.ts`<br>`internal/rules/production_sleep_loops/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `raw-fetch-abort-signal` | `docs/rules/raw-fetch-abort-signal.md` | `internal/rules/raw_fetch_abort_signal/rule.go` | `internal/rules/raw_fetch_abort_signal/rule_test.go` | `internal/rules/raw_fetch_abort_signal/testdata/index.ts`<br>`internal/rules/raw_fetch_abort_signal/testdata/tsconfig.json` | **FAIL** | **FAIL** | PASS | PASS |
| `raw-fetch-outside-adapter` | `docs/rules/raw-fetch-outside-adapter.md` | `internal/rules/raw_fetch_outside_adapter/rule.go` | `internal/rules/raw_fetch_outside_adapter/rule_test.go` | `internal/rules/raw_fetch_outside_adapter/testdata/index.ts`<br>`internal/rules/raw_fetch_outside_adapter/testdata/tsconfig.json` | **FAIL** | **FAIL** | PASS | PASS |
| `redundant-alias` | `docs/rules/redundant-alias.md` | `internal/rules/redundant_alias/rule.go` | `internal/rules/redundant_alias/rule_test.go` | `internal/rules/redundant_alias/testdata/index.ts`<br>`internal/rules/redundant_alias/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `require-because-in-comments` | `docs/rules/require-because-in-comments.md` | `internal/rules/require_because_in_comments/rule.go` | `internal/rules/require_because_in_comments/rule_test.go` | `internal/rules/require_because_in_comments/testdata/index.ts`<br>`internal/rules/require_because_in_comments/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `require-blank-lines-around-multiline-declarations` | `docs/rules/require-blank-lines-around-multiline-declarations.md` | `internal/rules/require_blank_lines_around_multiline_declarations/rule.go` | `internal/rules/require_blank_lines_around_multiline_declarations/rule_test.go` | `internal/rules/require_blank_lines_around_multiline_declarations/testdata/index.ts`<br>`internal/rules/require_blank_lines_around_multiline_declarations/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `require-callable-role-name-consistency` | `docs/rules/require-callable-role-name-consistency.md` | `internal/rules/require_callable_role_name_consistency/rule.go` | `internal/rules/require_callable_role_name_consistency/rule_test.go` | `internal/rules/require_callable_role_name_consistency/testdata/index.ts`<br>`internal/rules/require_callable_role_name_consistency/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `require-command-name-consistency` | `docs/rules/require-command-name-consistency.md` | `internal/rules/require_command_name_consistency/rule.go` | `internal/rules/require_command_name_consistency/rule_test.go` | `internal/rules/require_command_name_consistency/testdata/index.ts`<br>`internal/rules/require_command_name_consistency/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `require-construction-name-consistency` | `docs/rules/require-construction-name-consistency.md` | `internal/rules/require_construction_name_consistency/rule.go` | `internal/rules/require_construction_name_consistency/rule_test.go` | `internal/rules/require_construction_name_consistency/testdata/index.ts`<br>`internal/rules/require_construction_name_consistency/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `require-conversion-direction-consistency` | `docs/rules/require-conversion-direction-consistency.md` | `internal/rules/require_conversion_direction_consistency/rule.go` | `internal/rules/require_conversion_direction_consistency/rule_test.go` | `internal/rules/require_conversion_direction_consistency/testdata/index.ts`<br>`internal/rules/require_conversion_direction_consistency/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `require-lookup-totality-name-consistency` | `docs/rules/require-lookup-totality-name-consistency.md` | `internal/rules/require_lookup_totality_name_consistency/rule.go` | `internal/rules/require_lookup_totality_name_consistency/rule_test.go` | `internal/rules/require_lookup_totality_name_consistency/testdata/index.ts`<br>`internal/rules/require_lookup_totality_name_consistency/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `require-predicate-name-consistency` | `docs/rules/require-predicate-name-consistency.md` | `internal/rules/require_predicate_name_consistency/rule.go` | `internal/rules/require_predicate_name_consistency/rule_test.go` | `internal/rules/require_predicate_name_consistency/testdata/index.ts`<br>`internal/rules/require_predicate_name_consistency/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `require-result-cardinality-name-consistency` | `docs/rules/require-result-cardinality-name-consistency.md` | `internal/rules/require_result_cardinality_name_consistency/rule.go` | `internal/rules/require_result_cardinality_name_consistency/rule_test.go` | `internal/rules/require_result_cardinality_name_consistency/testdata/index.ts`<br>`internal/rules/require_result_cardinality_name_consistency/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |
| `require-result-shape-name-consistency` | `docs/rules/require-result-shape-name-consistency.md` | `internal/rules/require_result_shape_name_consistency/rule.go` | `internal/rules/require_result_shape_name_consistency/rule_test.go` | `internal/rules/require_result_shape_name_consistency/testdata/index.ts`<br>`internal/rules/require_result_shape_name_consistency/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `retry-without-jitter` | `docs/rules/retry-without-jitter.md` | `internal/rules/retry_without_jitter/rule.go` | `internal/rules/retry_without_jitter/rule_test.go` | `internal/rules/retry_without_jitter/testdata/index.ts`<br>`internal/rules/retry_without_jitter/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `schema-class-models` | `docs/rules/schema-class-models.md` | `internal/rules/schema_class_models/rule.go` | `internal/rules/schema_class_models/rule_test.go` | `internal/rules/schema_class_models/testdata/index.ts`<br>`internal/rules/schema_class_models/testdata/tsconfig.json` | PASS | PASS | PASS | PASS |
| `schema-error-class` | `docs/rules/schema-error-class.md` | `internal/rules/schema_error_class/schema_error_class.go` | `internal/rules/schema_error_class/rule_test.go` | `internal/rules/schema_error_class/testdata/clean.ts`<br>`internal/rules/schema_error_class/testdata/tsconfig.json`<br>`internal/rules/schema_error_class/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `schema-name-suffix` | `docs/rules/schema-name-suffix.md` | `internal/rules/schema_name_suffix/rule.go` | `internal/rules/schema_name_suffix/rule_test.go` | `internal/rules/schema_name_suffix/testdata/packages/effect/src/Schema.d.ts`<br>`internal/rules/schema_name_suffix/testdata/packages/effect/src/index.d.ts`<br>`internal/rules/schema_name_suffix/testdata/src/clean.ts`<br>`internal/rules/schema_name_suffix/testdata/src/violation.ts`<br>`internal/rules/schema_name_suffix/testdata/tsconfig.json`<br>`internal/rules/schema_name_suffix/testdata_v4/packages/effect/src/Schema.d.ts`<br>`internal/rules/schema_name_suffix/testdata_v4/packages/effect/src/index.d.ts`<br>`internal/rules/schema_name_suffix/testdata_v4/src/clean.ts`<br>`internal/rules/schema_name_suffix/testdata_v4/src/violation.ts`<br>`internal/rules/schema_name_suffix/testdata_v4/tsconfig.json` | PASS | PASS | PASS | PASS |
| `schema-optional-key` | `docs/rules/schema-optional-key.md` | `internal/rules/schema_optional_key/schema_optional_key.go` | `internal/rules/schema_optional_key/rule_test.go` | `internal/rules/schema_optional_key/testdata/clean.ts`<br>`internal/rules/schema_optional_key/testdata/tsconfig.json`<br>`internal/rules/schema_optional_key/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `schema-record-interface` | `docs/rules/schema-record-interface.md` | `internal/rules/schema_record_interface/schema_record_interface.go` | `internal/rules/schema_record_interface/rule_test.go` | `internal/rules/schema_record_interface/testdata/clean.ts`<br>`internal/rules/schema_record_interface/testdata/packages/effect/src/Equivalence.d.ts`<br>`internal/rules/schema_record_interface/testdata/packages/effect/src/Schema.d.ts`<br>`internal/rules/schema_record_interface/testdata/packages/effect/src/index.d.ts`<br>`internal/rules/schema_record_interface/testdata/tsconfig.json`<br>`internal/rules/schema_record_interface/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `scoped-background-work` | `docs/rules/scoped-background-work.md` | `internal/rules/scoped_background_work/scoped_background_work.go` | `internal/rules/scoped_background_work/rule_test.go` | `internal/rules/scoped_background_work/testdata/clean.ts`<br>`internal/rules/scoped_background_work/testdata/tsconfig.json`<br>`internal/rules/scoped_background_work/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `scoped-client-cache` | `docs/rules/scoped-client-cache.md` | `internal/rules/scoped_client_cache/scoped_client_cache.go` | `internal/rules/scoped_client_cache/rule_test.go` | `internal/rules/scoped_client_cache/testdata/clean.ts`<br>`internal/rules/scoped_client_cache/testdata/tsconfig.json`<br>`internal/rules/scoped_client_cache/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `service-method-effect-fn` | `docs/rules/service-method-effect-fn.md` | `internal/rules/service_method_effect_fn/service_method_effect_fn.go` | `internal/rules/service_method_effect_fn/rule_test.go` | `internal/rules/service_method_effect_fn/testdata/clean.ts`<br>`internal/rules/service_method_effect_fn/testdata/tsconfig.json`<br>`internal/rules/service_method_effect_fn/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `speculative-export` | `docs/rules/speculative-export.md` | `internal/rules/speculative_export/speculative_export.go` | `internal/rules/speculative_export/rule_test.go` | `internal/rules/speculative_export/testdata/clean.ts`<br>`internal/rules/speculative_export/testdata/consumer.ts`<br>`internal/rules/speculative_export/testdata/tsconfig.json`<br>`internal/rules/speculative_export/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `stream-pagination` | `docs/rules/stream-pagination.md` | `internal/rules/stream_pagination/stream_pagination.go` | `internal/rules/stream_pagination/rule_test.go` | `internal/rules/stream_pagination/testdata/clean.ts`<br>`internal/rules/stream_pagination/testdata/tsconfig.json`<br>`internal/rules/stream_pagination/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `test-clock-for-time` | `docs/rules/test-clock-for-time.md` | `internal/rules/test_clock_for_time/test_clock_for_time.go` | `internal/rules/test_clock_for_time/rule_test.go` | `internal/rules/test_clock_for_time/testdata/clean.ts`<br>`internal/rules/test_clock_for_time/testdata/tsconfig.json`<br>`internal/rules/test_clock_for_time/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `test-sleeps` | `docs/rules/test-sleeps.md` | `internal/rules/test_sleeps/test_sleeps.go` | `internal/rules/test_sleeps/rule_test.go` | `internal/rules/test_sleeps/testdata/clean.ts`<br>`internal/rules/test_sleeps/testdata/tsconfig.json`<br>`internal/rules/test_sleeps/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `typed-boundary-error` | `docs/rules/typed-boundary-error.md` | `internal/rules/typed_boundary_error/typed_boundary_error.go` | `internal/rules/typed_boundary_error/rule_test.go` | `internal/rules/typed_boundary_error/testdata/clean.ts`<br>`internal/rules/typed_boundary_error/testdata/tsconfig.json`<br>`internal/rules/typed_boundary_error/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `typed-error-recovery` | `docs/rules/typed-error-recovery.md` | `internal/rules/typed_error_recovery/typed_error_recovery.go` | `internal/rules/typed_error_recovery/rule_test.go` | `internal/rules/typed_error_recovery/testdata/clean.ts`<br>`internal/rules/typed_error_recovery/testdata/effect.d.ts`<br>`internal/rules/typed_error_recovery/testdata/tsconfig.json`<br>`internal/rules/typed_error_recovery/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `typescript-namespaces` | `docs/rules/typescript-namespaces.md` | `internal/rules/typescript_namespaces/typescript_namespaces.go` | `internal/rules/typescript_namespaces/rule_test.go` | `internal/rules/typescript_namespaces/testdata/clean.ts`<br>`internal/rules/typescript_namespaces/testdata/tsconfig.json`<br>`internal/rules/typescript_namespaces/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `unbounded-stream-buffer` | `docs/rules/unbounded-stream-buffer.md` | `internal/rules/unbounded_stream_buffer/unbounded_stream_buffer.go` | `internal/rules/unbounded_stream_buffer/rule_test.go` | `internal/rules/unbounded_stream_buffer/testdata/clean.ts`<br>`internal/rules/unbounded_stream_buffer/testdata/tsconfig.json`<br>`internal/rules/unbounded_stream_buffer/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `unbounded-stream-collect` | `docs/rules/unbounded-stream-collect.md` | `internal/rules/unbounded_stream_collect/unbounded_stream_collect.go` | `internal/rules/unbounded_stream_collect/rule_test.go` | `internal/rules/unbounded_stream_collect/testdata/clean.ts`<br>`internal/rules/unbounded_stream_collect/testdata/tsconfig.json`<br>`internal/rules/unbounded_stream_collect/testdata/violation.ts` | **FAIL** | PASS | PASS | PASS |
| `unsafe-casts` | `docs/rules/unsafe-casts.md` | `internal/rules/unsafe_casts/unsafe_casts.go` | `internal/rules/unsafe_casts/rule_test.go` | `internal/rules/unsafe_casts/testdata/clean.ts`<br>`internal/rules/unsafe_casts/testdata/tsconfig.json`<br>`internal/rules/unsafe_casts/testdata/violation.ts` | PASS | PASS | PASS | PASS |
| `unused-field` | `docs/rules/unused-field.md` | `internal/rules/unused_field/unused_field.go` | `internal/rules/unused_field/rule_test.go` | `internal/rules/unused_field/testdata/src/clean.ts`<br>`internal/rules/unused_field/testdata/src/computed.ts`<br>`internal/rules/unused_field/testdata/src/merged-a.ts`<br>`internal/rules/unused_field/testdata/src/merged-b.ts`<br>`internal/rules/unused_field/testdata/src/violation.ts`<br>`internal/rules/unused_field/testdata/tsconfig.json` | **FAIL** | PASS | PASS | PASS |

## Failures

### `boundary-schema-decode` — `What it does`

- **What it does: FAIL.** The receiver test is not a finite receiver-name test. It is a case-insensitive raw-text substring regex over the whole receiver, so names such as `myRequestClient` and `bodyguard` qualify. `JSON`, boundary receivers, and decoders are not resolved to imports or symbols. Only identifier/property-access callees are recognized after parentheses/assertion/non-null unwrapping; element access is not. Any matching decoder call anywhere in the nearest enclosing function suppresses the report, including inside a nested function or unrelated branch and regardless of data flow; a matching ancestor wrapper also suppresses even outside a function. **Correction:** describe these textual call gates, the substring receiver match, the exact finite decoder-name last-segment match, the no-symbol/no-flow behavior, the full nearest-function recursive scan (including nested functions), and the ancestor-call exemption.

### `bounded-retry-schedule` — `What it does`

- **What it does: FAIL.** “Unbounded schedule” is only a textual/structural heuristic. The listener accepts bare `retry(...)` or direct property access on the exact identifier `Effect`; it does no import/symbol resolution. With two or more arguments it checks argument 2, except an object-literal argument 1 is treated as the policy; with one argument it exempts arrow/function expressions, treats other non-objects as policy, and exempts no-argument calls. A non-object policy is “bounded” when its raw text has any listed bound word. An object is bounded by default when it has no `schedule`; only direct property assignments count, `times` counts only for a numeric literal or identifier, `while`/`until` count regardless of value, and `schedule` is searched textually. The 300-character waiver is also raw source and can occur in comments, strings, or unrelated code. **Correction:** replace the semantic “is an unbounded schedule” wording with the exact argument-selection, object-property, raw-text bound, receiver, and waiver heuristics and state that no symbols or actual schedule values are resolved.

### `cache-per-request` — `What it does`

- **What it does: FAIL.** The description omits important textual and function-form boundaries. Only direct property-access `make`/`makeWith` calls are checked; the receiver’s raw text merely has to end in case-sensitive `Cache`, so unrelated values such as `NotACache` qualify, with no import/symbol/type check. The nearest enclosing function controls the result. A zero-parameter function is module-scope only when it is a direct source-file/module-block function declaration, or a direct function-valued variable whose variable statement is at source-file scope. Methods, nested functions, and other forms are non-module-scope. The ancestor-cache exemption is textual and crosses intervening function boundaries. **Correction:** add the exact callee/receiver gate, absence of semantic resolution, nearest-function rule, exact accepted module-scope forms, and unbounded ancestor exemption.

### `cache-preference` — `What it does`

- **What it does: FAIL.** `Map` is an exact textual identifier with no global-symbol check, and wrappers between `new Map()` and its parent prevent binding extraction. The second listener also accepts bare `set(...)`, not only `.set()`. Its exact case-insensitive TTL names are `expires`, `expire`, optional `At` variants, `expiry`, `ttl`, `deadline`, `validUntil`, and `staleAt`; only direct property assignments and shorthand properties in argument 2 count. One matching `make`/`makeWith` call anywhere in the file suppresses every TTL-set report when its raw receiver text ends in `Cache`, even if unrelated; no Effect import or Cache symbol is required. **Correction:** state these exact node forms and TTL names, the second-argument rule, the whole-file textual exemption, and the lack of symbol resolution.

### `closed-abstraction` — `What it does`

- **What it does: FAIL.** The broad algorithm is stated, but its false-positive boundaries are not. Functions are collected project-wide from all nested named function declarations and only directly variable-bound function-like nodes; methods and wrapped variable initializers are not owners. Files whose path contains `node_modules` and `.d.ts` files are excluded. Type ownership is a case-sensitive raw substring search over each function’s full source, so comments, strings, and longer identifiers count. External-use counting is lexical source text, including comments/strings, over the same file set; it uses ASCII word bytes for ordinary names and subtracts one occurrence without tying that occurrence to the declaration. There is no symbol/reference resolution. **Correction:** retain the current algorithm summary but add the collected function forms, file exclusions, raw-substring false positives, exact lexical reference counting, nested traversal, and lack of semantic resolution.

### `config-refined-values` — `What it does`

- **What it does: FAIL.** The candidate is any direct property-access `string` call whose raw receiver text case-sensitively ends in `Config`, not specifically the Effect `Config` symbol. Only a non-empty string-literal-like first argument is checked after limited unwrapping. Suffixes are raw end matches with no separator boundary, so any key ending in `id` (for example `invalid`) qualifies. Suppression comes from any ancestor identifier/property-access call with a listed last name, regardless of receiver, symbol, purpose, or data flow, and can extend to the source root. **Correction:** add the receiver-suffix/callee node gate, first-literal-only rule, no-boundary suffix behavior, arbitrary-ancestor exemption, and lack of import/symbol resolution.

### `dependent-layer-merge` — `What it does`

- **What it does: FAIL.** The type-channel and certainty description is accurate but omits entry and traversal boundaries. Only direct property-access `merge`/`mergeAll` calls whose raw receiver text case-sensitively ends in `Layer` are checked; the `Layer` value/import is not resolved. Each top-level argument is unwrapped, and a top-level array literal is flattened exactly one level; array elements are then checked as written. Recognition uses a case-sensitive `"Layer"` substring in `TypeToString`, object-reference flags, and the first and third of at least three type arguments. Certainty rejects only direct `any`, `unknown`, type-parameter, or `never` flags; it does not recursively prove certainty. All ordered pairs of recognized values are tested and one report is emitted on the call. **Correction:** add these exact receiver, array-flattening, type-rendering, direct-flag, ordered-pair, and no-symbol-resolution limits.

### `duplicate-shape` — `What it does`

- **What it does: FAIL.** It does not report every declaration that has a match. It chooses the lexicographically smallest `filename:name` key for each canonical shape and reports every other matching declaration; the chosen declaration is silent. Same-file declarations with the same name share a key and can also be suppressed. Project scanning excludes paths containing `node_modules` and `.d.ts` files. All comparison is raw source normalization with no alias or symbol resolution; lowercase `readonly` is removed even inside a longer token, and ordering is normalized only for separators recognized at top-level by the simple delimiter-depth scanner. **Correction:** state the deterministic first-entry exemption/key collision boundary and file exclusions, and explicitly call the process textual rather than semantic.

### `effect-fn-name` — `What it does`

- **What it does: FAIL.** Invalid literal names are checked only when an `Effect.fn(...)` call is itself immediately invoked. A non-invoked `Effect.fn("load")`, `Effect.fn()`, or multi-argument `Effect.fn("load", fn)` is not reported. A directly passed arrow, function expression, or object first argument is reported as unnamed even without an outer invocation. The receiver is any raw text ending in case-sensitive `Effect`, with no import/symbol check, and only property access is recognized. The regex validates only a two-part prefix (`^[^.\s]+\.[^.\s]+`), not the rest of the string. **Correction:** describe the outer-call and direct-argument listener branches, their missing-name behavior, the receiver/callee gate, no semantic resolution, and prefix-only qualification.

### `effect-test-style` — `What it does`

- **What it does: FAIL.** The file gate is only a raw `@effect/vitest` substring, so comments/strings qualify and no import binding is resolved. Test recognition is textual and accepts direct `it`, the five listed direct properties on identifier `it`, or the immediate outer call of `it.each(...)`; aliases, element access, chained variants, and symbols are not recognized. Arguments are scanned right-to-left and only the first inline arrow/function expression is treated as the callback; a callback identifier is ignored. Effect detection is either a case-sensitive `"Effect"` substring in the rendered return type or a raw `"Effect."` substring anywhere in the callback source, including nested/unreturned/comment/string text. **Correction:** add the raw file gate, exact callee forms, rightmost-inline-callback limit, textual fallback false positives, and lack of import/symbol resolution.

### `function-derived-model` — `What it does`

- **What it does: FAIL.** The rule does not prove a “sole function role” or count any model references. One matching function is enough even when the type is used by many other functions. It scans project-wide nested named function declarations and only directly variable-bound function-like nodes, excluding paths containing `node_modules` and `.d.ts` files; methods and wrapped variable initializers are not candidates. Suffix matching is case-sensitive, stem/function matching is case-insensitive, and the full type-name occurrence in function source is a case-sensitive raw substring that can come from comments, strings, or longer identifiers. There is no symbol resolution. **Correction:** remove or qualify “sole,” and state the exact function forms, file exclusions, matching case rules, raw-source containment, project traversal, and no reference/symbol analysis.

### `http-status-decode-order` — `What it does`

- **What it does: FAIL.** The description sounds HTTP- and order-semantic but omits material syntactic gates and false-positive boundaries. The call listener accepts final callee names without symbol/import/receiver checks. The six body names are checked in any nearest function. It also treats `decodeUnknown`, `decodeUnknownEffect`, `decode`, `decodeEffect`, `schemaBodyJson`, `schemaJson`, and `schemaNoBody` as decode targets, but only when a recursive whole-function scan finds a body/classifier call or a call named `execute`, `get`, `post`, `put`, `patch`, or `del`. A target with no enclosing function is exempt. “Before” is recursive AST visitation order, not control flow. Any earlier property access named `status`, `ok`, or `statusText`, or call named `filterStatusOk`, `filterStatus`, or `matchStatus`, suppresses the report, even on unrelated receivers, branches, or nested code. **Proposed correction:** replace the first paragraph with this exact target list, decoder-only HTTP-looking gate, nearest-function gate, finite classifier list, preorder traversal rule, and the lack of symbol, receiver, data-flow, and control-flow certainty.

### `idempotent-retry` — `What it does`

- **What it does: FAIL.** “Effect `retry`” and “a mutation verb such as `save`” hide decisive finite and syntactic limits. Calls are recognized only through the hand-built top-level import table: named or namespace imports from exact module `effect`, or named imports from any `effect/`-prefixed module. Accepted callee paths are imported `Effect.retry`/`retryOrElse`, namespace-root `X.Effect.retry`/`retryOrElse`, or direct named imports rendered as `Effect.retry`/`Effect.retryOrElse`. There is no checker resolution and shadowing is ignored. The nearest function-like must have a direct identifier/string/numeric name, be directly initialized in an identifier variable declaration, or be directly in a property assignment; an anonymous nearest callback stops the search. Mutation prefixes are case-insensitive `create|insert|update|upsert|delete|remove|write|save|put|post|patch|send|publish|enqueue|dispatch|mutate`. The explicit allowed prefixes are `get|list|find|read|lookup|fetch|resolve|load|query|check`; all other names are also silently allowed. **Proposed correction:** state these exact import/callee forms, nearest-function naming gates, both finite prefix lists, prefix/case behavior, anonymous-function boundary, and lack of semantic symbol or idempotency proof.

### `layer-forever-acquisition` — `What it does`

- **What it does: FAIL.** The target and forever/run/fork combinations are right, but “imported” implies stronger resolution than exists and omits important entry gates. A hand-built top-level table accepts named or namespace imports from `effect` and named imports from any `effect/`-prefixed module. It recognizes only direct identifier/property paths; it does not use checker symbols and ignores later shadowing. It also accepts direct imported API names through textual prefix trimming. `Layer.effect` inspects only argument 2; `effectDiscard` and `effectContext` inspect only argument 1; missing arguments are exempt. The descendant scan is unlimited and treats a bare API reference as presence, including unrelated nested code. Any recognized `Effect.forkScoped` anywhere in that one argument exempts every forever occurrence. **Proposed correction:** retain the finite API lists but add the exact import/path forms, positional acquisition arguments, descendant/reference behavior, no symbol/shadow certainty, and the unrelated-fork exemption boundary.

### `missing-rationale` — `What it does`

- **What it does: FAIL.** The rationale window is broadly described correctly, but the claimed declaration categories are not source-faithful enough. A data interface means any heritage clause or at least one property/index signature; method-only and empty interfaces are exempt. A type alias is exempt only when its direct type node is a function or constructor type. Every enum qualifies. An “Effect data class” is actually a raw regex over the entire trimmed class text: `extends (Data|Schema).(Class|Error|ErrorClass|Opaque|TaggedClass|TaggedError|TaggedErrorClass|asClass)` or any `extends Identifier.extend`; there is no import/symbol check. An exported Schema variable requires an identifier binding and checker type text containing `Schema<` anywhere or starting `Schema.`. Only top-level statements are scanned at EOF. The raw range starts at the immediately previous top-level statement’s `End`, so trailing comments can count; all `//` captures are joined, any case-insensitive `because` substring counts, and direct adjacency is not required. Multiple declarations in one exported variable statement share the same rationale window. The quoted report is also dynamic: it begins with the actual declaration name, not always `RequestData`. **Proposed correction:** define each structural gate and heuristic above, say explicitly that class recognition is raw regex rather than Effect symbol resolution, state the identifier/type-rendering and top-level limits, and render the message as `<name> lacks ...`.

### `no-blank-lines-between-single-line-declarations` — `What it does`

- **What it does: FAIL.** “Declarations” is materially broader than the finite listener/predicate set. Only variable statements, function declarations, class declarations, interface declarations, type aliases, enums, and module declarations qualify; object/class methods, properties, and other declaration nodes do not. Both nodes must be immediate neighbors in the same statement list whose parent `CanHaveStatements`, and the current node must have some function-like ancestor before the source file. “Single-line” is a raw trimmed-node check for `\n`. The gap is textual `\n[ \t]*\r?\n`; an intervening comment does not inherently exempt the pair if that pattern still occurs. The second declaration is reported. Multi-line neighbors and top-level nodes are only exempt; separators around multi-line declarations are not enforced despite the help prose saying they are “required.” **Proposed correction:** name the seven declaration kinds, immediate-same-statement-list and function-ancestor gates, raw newline/blank regex, comment behavior, report location, and clarify that the rule does not enforce separators around multi-line declarations.

### `no-callbacks` — `What it does`

- **What it does — FAIL.** “Selected function-type annotations” hides material gates. Function-type nodes qualify only after climbing parenthesized/union/intersection wrappers and finding that wrapper as the direct type of a variable or property declaration, type alias, or property signature. A variable/property annotation is skipped when initialized by a function expression or arrow. The other exact listener forms are function declarations, function expressions, arrows, method declarations, method signatures, and call signatures. All forms require a checker signature whose return type directly has `TypeFlagsVoid`; at least one checked parameter type must have a call signature. A rest parameter also matches when its checked type is an object reference whose first type argument is callable. Missing signatures/types abstain. Ambient ancestors and declaration files are skipped. **Proposed correction:** replace “selected” with these exact node, annotation-parent, semantic-type, rest-parameter, ambient, and certainty gates.

### `no-duplicate-function-names` — `What it does`, `When to use it`

- **What it does — FAIL.** The broad description omits the finite scan and overstates “variables initialized with functions.” At each EOF, the rule scans only top-level statements. It accepts identifier-named function declarations and identifier variable declarations whose **direct, unwrapped** initializer is a function expression or arrow. It ignores same-file matches. The cross-file index excludes declaration files and paths containing `/node_modules/`. Matching is same text name plus bidirectional assignability of the whole value types; bodies are never compared. **Proposed correction:** state these identifier/direct-initializer, top-level, cross-file exclusion, whole-type, and no-body-comparison limits. Replace “different signatures are allowed” with “types that are not mutually assignable in both directions are allowed.”
- **When to use it — FAIL.** “Find semantic copies” is not established by this rule. Same name and mutually assignable callable types do not prove equal behavior. **Proposed correction:** say it finds same-name, mutually assignable top-level callables that are candidates for consolidation, not semantic copies.

### `no-duplicate-if-bodies` — `What it does`

- **What it does — FAIL.** “Identical” is not exact enough. Bodies are compared by token text after unwrapping a block with exactly one statement; scanner trivia and semicolon tokens are ignored. For separate statements, both must have no `else`, must be immediately adjacent children of an AST block (not a source file), and only the later body must syntactically end, recursively through its last block statement, in `break`, `continue`, `return`, or `throw`. For an `else if`, only the parent `then` and child `then` fingerprints are compared; there is no exit gate. **Proposed correction:** document this textual token fingerprint, one-statement-block normalization, exact adjacency/container gates, finite exit forms, and the different `else if` gate.

### `no-error-type` — `What it does`

- **What it does — FAIL.** The rule does not inspect every type reference whose underlying type resolves to built-in `Error`. It listens only to `TypeReference` nodes whose written name is `Error`, or a qualified name whose right side is `Error`. It then requires that name’s symbol to be named `Error` and either have a declaration rooted in a `lib.*.d.ts` source file or have no declarations and be transient. It does not follow a differently named type alias such as `E`. A shadowing local `Error` is allowed, but a merged symbol with any lib declaration still matches. **Proposed correction:** describe the written-name and symbol-declaration check, the lack of alias expansion, and the merge boundary instead of saying all references that “resolve” to the built-in type.

### `no-explicit-any-return` — `What it does`

- **What it does — FAIL.** The check is syntactic, not resolved-type based, and the listener set is finite. It recursively searches the explicit `Type()` AST for an `AnyKeyword` on function declarations, function expressions, arrows, method declarations, method signatures, call signatures, function types, and get accessors. It does not check inferred returns or expand aliases that resolve to `any`. Each listened node reports independently, so a nested function-type return can also report separately from an enclosing return annotation. **Proposed correction:** enumerate these node forms and state the syntax-only recursion, alias/inference limits, and independent nested reports.

### `no-first-party-schema-declare` — `What it does`

- **What it does — FAIL.** Most type gates are accurate, but the document omits a key false-positive boundary: the callee is matched purely by the text shape `Schema.declare`; no Effect import or callee symbol is resolved. A local lookalike named `Schema` therefore matches. Only the first argument and the first call signature are inspected. The asserted type must be non-nil, non-type-parameter, non-callable, and have a symbol with at least one qualifying declaration. “First-party” means a non-declaration source file whose normalized path lacks `/node_modules/`, not project ownership. Any qualifying declaration on a merged symbol is enough. The primitive exemption requires a direct intersection with more than one part and at least one direct primitive keyword from the listed set. **Proposed correction:** add the textual callee/no-import-resolution behavior, first-argument/first-signature limits, path-based ownership and merged-symbol rule, and the direct-intersection constraint.

### `no-immediate-effect-sync` — `What it does`

- **What it does — FAIL.** The main sequence is accurate, but “A local lookalike is allowed” is too broad. Import recognition records local spellings from syntax and later matches callees by text, not checker symbols. If a recognized imported spelling is shadowed inside a nested scope by a local lookalike, that lookalike can still match. With no recognized import syntax, a lookalike is allowed. The `runSync` first argument must be an identifier; extra arguments do not matter. Its nearest containing statement must be a direct child of a block or source file. The immediately previous statement must be a variable statement containing the same checker symbol with a direct call initializer after unwrapping parentheses, `as`, `satisfies`, and non-null expressions. Reference counting recursively traverses the entire containing block/file, including nested scopes, and must count exactly the declaration plus run argument. **Proposed correction:** qualify the lookalike statement and document textual import-spelling matching, first-argument behavior, exact container/previous-statement gates, initializer wrappers, and recursive reference-count boundary.

### `no-inline-closures` — `What it does`

- Reason: “passed to an external package” is broader than `isExternalPackageArgument`. The exemption reaches a call or `new` argument only through parentheses, `as`, `satisfies`, object literals, property assignments, and array literals. It does not traverse a non-null wrapper or other expression shapes. It then checks the callee symbol (the member name for property access), resolves an alias, and requires a declaration under `/node_modules/`, excluding `lib.*.d.ts`. Thus some arrows passed indirectly to an external call are still reported.
- Proposed correction: state the supported argument-wrapper list and the symbol/declaration gate; explicitly say that unsupported wrappers, including non-null, do not receive the external-package exemption. Keep the existing effective-parent and direct nested-arrow currying limits.

### `no-manual-type-dispatch` — `What it does`

- Reason: “shares an identifier” does not expose that sharing is purely textual. `identifierNames` collects every identifier node’s text, including property-name identifiers, and never resolves symbols. Different values such as `a.kind`, `b.kind`, and `c.kind` can therefore form a chain solely through the spelling `kind`. The rest of the page correctly states the direct-block gate, adjacency, first-node-only report, minimum length three, no-`else` guard, and exit requirement (direct or recursively last-in-block `break`, `continue`, `return`, or `throw`).
- Proposed correction: change this to “each adjacent pair has at least one identical identifier spelling anywhere in its conditions; names are compared textually, including property names, without symbol resolution.”

### `no-mutable-array-methods` — `What it does`

- Reason: “calls to these methods” is broader than the sole listener shape. The implementation accepts only a `CallExpression` whose callee is a dotted `PropertyAccessExpression`. It does not check element access such as `values["push"](3)`. Conversely it does not resolve the method symbol: any listed property spelling on a receiver whose checker type is array-like reports.
- Proposed correction: say “Reports direct dotted property-access calls with these names when the receiver’s checker type is array-like. Computed element access is not checked, and the method declaration itself is not resolved.” Keep the existing exact nine-name list.

### `no-mutation` — `What it does`

- Reason: the ownership summary omits material certainty and composite-type gates. Bare targets use an alias-resolved target symbol, but property/element targets classify the receiver type. For unions, nullish members are discarded when any non-nullish member exists, and the target is uncontrolled only when every relevant member is uncontrolled. For intersections, one uncontrolled member makes the whole target uncontrolled. Missing symbols/declarations and recursion cycles default to controlled/reportable. Consequently a receiver intersection with both first-party and third-party members can be allowed despite the broad “mutate first-party data” description.
- Proposed correction: retain the exact `lib.es*`/`lib.decorators*`/`lib.d.ts` boundary, then add the bare-target versus receiver-type distinction and the union/intersection certainty rules. State that unknown ownership defaults to reporting and that an uncontrolled intersection constituent exempts the property/element mutation.

### `no-new-error` — `What it does`

- What it does: **FAIL**
  - Reason: `no_new_error.go:12-16` performs a text-only check: every `NewExpression` whose callee is an identifier spelled `Error` is reported. It does not resolve the global `Error` symbol. Thus a shadowed/custom class named `Error` is reported, while `new globalThis.Error()` and an alias such as `new ErrorAlias()` are not. “Direct `new Error(...)`” does not state this false-positive/false-negative boundary, and “Other constructors are allowed” can be read semantically rather than by callee spelling.
  - Proposed correction: “Reports every `new` expression whose callee is a bare identifier spelled `Error`. This is a textual check with no symbol resolution: a shadowed/custom `Error` is also reported, while aliases and member accesses such as `globalThis.Error` are not.”

### `no-reexports` — `What it does`

- What it does: **FAIL**
  - Reason: “Reports exports that forward bindings from another module” is broader and more semantic than `rule.go:11-50,55-82`. The rule always handles binding-bearing `export ... from` forms, but local export clauses and export assignments use a textual set built only from top-level `ImportDeclaration` default/namespace/named local names. There is no checker resolution. `ImportEqualsDeclaration` bindings are not collected, and an export assignment is checked only when its whole expression is a bare imported identifier; member or compound expressions are not checked.
  - Proposed correction: “Reports `export *`, namespace export clauses, and each named `export { ... } from` specifier. It also reports named local export specifiers and bare-identifier export assignments when their local text matches a default, namespace, or named binding from a top-level ES import. It does not use symbol resolution, collect `import =` bindings, or inspect member/compound export-assignment expressions.”

### `no-trivial-effect-fn` — `What it does`

- What it does: **FAIL**
  - Reason: the summary omits material entry and exemption boundaries in `rule.go:12-190`. The variable name must be an identifier; the curried outer call must have exactly one direct generator function-expression argument; the `fn` property symbol must resolve (with alias unwrapping) to an Effect declaration path; the generator must contain exactly one `return yield* <call>`. Non-defaulted identifier parameters are compared textually and positionally, but rest parameters are accepted when forwarded as matching spreads, so “simple parameters” is incomplete. The “recognized service operation” exemption is not semantic service detection: it uses a limited ancestor-class child scan, or exempts the same variable symbol when it is a direct shorthand/property initializer in an object passed through only parenthesized/call ancestors to **any** `.of(...)` call or to symbol-resolved `Effect.succeed(...)`. The unrestricted `.of` name is a notable false-negative boundary.
  - Proposed correction: state those entry gates and replace “except wrappers recognized as service operations” with the exact two service heuristics, including rest/spread support, symbol/path certainty for `Effect.fn`/`Effect.succeed`, and the unrestricted `.of` exemption.

### `no-undefined` — `What it does`

- What it does: **FAIL**
  - Reason: the prose omits important finite and textual boundaries in `rule.go:14-60,71-109`. “Comparisons” are only `==`, `===`, `!=`, and `!==`. Expression detection is a bare identifier spelled `undefined` after unwrapping parentheses, `as`, `satisfies`, type assertions, and non-null assertions; no symbol/type resolution occurs, so shadowed `undefined` is reported and `void 0` or an alias is not. Type detection recursively finds the literal `UndefinedKeyword` AST anywhere, but not a type alias whose resolved type is undefined. Return-type listeners are limited to function declarations/expressions, arrows, method declarations/signatures, call signatures, function types, and getters. Property checks cover property signatures and mapped types, not class property declarations. Direct-return checks cover explicit return expressions and expression-bodied arrows only; fallthrough and all bare `return;` statements are ignored regardless of declared return type.
  - Proposed correction: replace the broad summary with those exact equality operators, textual unwrap set, recursive syntactic type check, finite return/property node forms, and direct-return limits.

### `no-unsafe-effect-apis` — `What it does`

- What it does: **FAIL**
  - Reason: “value-use identifiers” is false for `rule.go:21-49`: ordinary type-reference identifiers are not excluded and are reported if their resolved Effect symbol name contains `unsafe`. Only identifiers whose **direct parent** is an import/export specifier, import clause, namespace import/export, or `TypeQuery` are excluded. Also, the property- and element-access listeners have no type-query exclusion, so `typeof unsafeBare` is excluded while `typeof Effect.unsafeMember` is still reported. The check resolves one alias, matches `unsafe` in `symbol.Name` case-insensitively, and requires a declaration file path containing `/node_modules/effect/` or ending `/effect/index.d.ts`; string-literal-like element keys are the only computed keys checked.
  - Proposed correction: “Reports resolved identifiers in both value and ordinary type-reference positions, except the listed direct-parent import/export/type-query forms. It also reports property accesses and string-literal-like element accesses, including those nested in a type query. After one alias resolution, the symbol name must contain `unsafe` case-insensitively and at least one declaration must be under `/node_modules/effect/` or end in `/effect/index.d.ts`.”

### `no-value-aliases` — `What it does`

**Failure:** “non-optional property chain” is broader than the implementation, and the wrapper claim is unqualified. The listener checks only identifier-named `const` variable declarations. The initializer, as a whole, is stripped of parentheses, `as`, `satisfies`, angle-bracket assertion, and non-null wrappers. It then accepts only identifiers and dot `PropertyAccessExpression` chains. It does not accept element access such as `source["value"]`, and it does not unwrap a receiver inside a dot chain, so `(source).value` or `(source as T).value` does not report even though `(source.value)` does.

**Proposed correction:** Say “Reports an identifier-named `const` whose whole initializer, after outer wrappers are removed, is a bare identifier or a non-optional dot-property chain whose receivers are themselves unwrapped dot-property accesses/identifiers. Element access and inner receiver wrappers are not checked.”

### `no-weak-map` — `What it does`

**Failure:** The doc limits reports to built-in or declaration-file identifiers, but the identifier listener also reports an unresolved identifier whose text is exactly `WeakMap` (`symbol == nil`). For a resolved symbol, one declaration in a non-declaration file outside a path containing `/node_modules/` is enough to exempt the symbol; otherwise it reports. This is a textual identifier entry gate plus a semantic exemption, not a requirement that a declaration-file symbol exist.

**Proposed correction:** “Reports every identifier token spelled `WeakMap` unless its resolved symbol has at least one declaration in a non-`.d.ts`, non-`node_modules` source file. This includes built-ins, declaration-file/package symbols, and unresolved `WeakMap` identifiers.”

### `observable-worker-failure` — `What it does`

**Failure:** “recognizes the real Effect exports” overstates the certainty check. For every call, the code unwraps outer parentheses/`as`/`satisfies`, resolves either the direct callee or a property name, dereferences one alias, requires symbol name `ignore` or `ignoreCause`, and accepts any declaration whose normalized file path contains `/node_modules/effect/` **or merely ends in `/effect/index.d.ts`**. It does not prove the module/package identity. An unrelated declaration at the fallback path can report; an Effect-shaped declaration elsewhere cannot. Logging is purely textual, not symbol-resolved: bare `log|info|warn|error|debug|trace`, or property methods with those names plus `fatal`. The recursive scan covers the entire nearest function-like subtree, including nested functions; outside a function always reports.

**Proposed correction:** Replace “real Effect exports” with the exact declaration-path rule and state that logging recognition is name-only. Keep the current finite name lists and nearest-subtree wording.

### `parameter-bag` — `What it does`

**Failure:** “matching parameter has a named model type” implies resolved overload/type semantics that the implementation does not perform. It resolves the callee/name symbol (one alias hop), walks symbol declarations, and uses the first recognized named function/method or named variable initialized with an arrow/function expression. It does not call `GetResolvedSignature`. The positional parameter annotation must syntactically be a parenthesized `TypeReferenceNode`; only the terminal identifier of an identifier/qualified type name is used. It never verifies that this is a model/object type. Arguments report only when that same argument index is, after outer parentheses/`as`/`satisfies`, an object literal. Anonymous default functions, inferred parameter types, unresolved callees, and nonliteral values remain outside the check.

**Proposed correction:** Describe this as a declaration-based, positional syntactic check, not the matching resolved parameter/model type. State that overload selection is not resolved and that only explicit type-reference syntax qualifies.

### `pass-through-conversion` — `What it does`

**Failure:** The detailed description still overstates “named” and semantic identity. The four listeners cover function declarations, arrows, function expressions, and methods, but a name is accepted only from an identifier function/method name or an identifier parent variable; a string/computed named method is skipped. Source and return annotations must be syntactic type references, and “different” compares only their terminal text, so `A.Model` to `B.Model` is skipped. Same shape requires nonzero equal checker property sets by name, but field types are compared by `TypeToString`, not checker identity/assignability. The body must be exactly an object expression or a block with one return; every property must be a same-name `parameter.field` assignment or a direct spread of that parameter, with mixtures allowed.

**Proposed correction:** Narrow “named function or method” to identifier-named forms/identifier variable initializers. Say explicit type-reference annotations with different terminal names, and say rendered property types must match. Retain the exact one-statement/nonempty object and copy/spread limits.

### `prefer-composed-callbacks` — `What it does`

**Failure:** “inline ... arrow callback” is broader than the entry gate. The arrow node itself must be the direct child of a call expression; a wrapper around the arrow makes its parent the wrapper and bypasses the rule. It must have exactly one identifier parameter and a non-block body. Parameter use is symbol-resolved and recursively searched only inside call arguments anywhere in the expression subtree. A body that is itself a call with exactly one argument equal to the parameter symbol after outer wrappers is exempt, regardless of the callee. Destructured parameters are not checked.

**Proposed correction:** Say “a direct, unwrapped call argument that is a one-identifier-parameter, expression-bodied arrow.” State the symbol-resolved recursive argument search and the exact one-argument direct-forward exemption.

### `prefer-conditional-return` — `What it does`

**Failure:** It does not report every qualifying `if`. The sole listener is `Block`, and it inspects only `if` nodes directly in that block's statement list. A top-level source-file `if`, or a qualifying nested `if` used directly as another statement's unbraced child, is not checked. Each branch must be a direct return or a block containing exactly one return; absent `else`, only the immediately next block statement is tried. Each returned source slice must contain no `\n`, have Go byte length at most 100 (not necessarily 100 characters), contain no recursive `yield`, and not unwrap to a conditional expression.

**Proposed correction:** Say “Reports a qualifying `if` that is a direct statement of an AST block.” Replace “100 characters” with “100 source bytes” (or change the implementation). Keep the exact branch/next-statement and eligibility limits.

### `prefer-curried-data-last-functions` — `What it does`

**Failure:** The phrase “callback arguments” claims a semantic check that is absent. The four function-form listeners report any rest parameter or more than one runtime parameter after excluding an identifier parameter literally named `this`. Arrows/function expressions with a callable contextual type are exempt. For the named-use exemption, the implementation scans every non-declaration source file and requires at least one reference; every same-symbol identifier reference, after only the listed wrappers, must be a direct argument to a call whose resolved signature declaration is from **any** declaration file or the default library. It does not inspect the selected parameter or prove that it is callable, so passing the function to a declaration-file parameter typed `unknown` is also exempt. An unused named function is not exempt.

**Proposed correction:** Replace “callback arguments” with “direct call arguments to calls whose resolved signature is declared in a `.d.ts` or default-library file,” state that at least one such reference is required, and avoid claiming the receiving parameter is a callback type.

### `prefer-effect-array-append-all` — `What it does`, `When to use it`

- What it does: **FAIL**. `isNonEmptyArrayBranch` does not establish that a branch is non-empty. It returns true for every non-array-literal expression, including an array identifier that can be empty. The only certainty is that one arm is exactly an empty array literal and the other arm is not an empty array literal. Proposed correction: replace “a non-empty branch” with “a branch that is not an empty array literal,” and describe the check as purely syntactic after parenthesis unwrapping.
- When to use it: **FAIL**. The rule also reports the reversed arms (`condition ? [] : values`), so the appended branch need not be selected when the written condition is true; it also does not prove the other branch has elements. Proposed correction: “Use it for a conditional array spread whose one arm is exactly `[]` and whose other arm is not an empty array literal.”

### `prefer-effect-function-constant` — `What it does`, `When to use it`

- What it does: **FAIL**. The page claims all zero-argument arrow/function expressions of the stated return shape, but the listener exempts async functions, generator function expressions, and every generic function. It also accepts only expression-bodied arrows or a block containing exactly one `return`, only the finite literal kinds in `primitive`, or an identifier for a single-declaration earlier same-file `const`. Proposed correction: state these entry gates and finite accepted value shapes.
- When to use it: **FAIL**. “A zero-argument function always returns the same stable value” includes async, generator, and generic thunks that the rule deliberately allows; replacing an async/generator thunk with `Function.constant` also changes its contract. Proposed correction: limit the advice to synchronous, non-generator, non-generic arrow/function expressions with the rule’s exact single-return shape.

### `prefer-effect-schema-constructor` — `What it does`

- What it does: **FAIL**. “A string `_tag`” is broader than `tagValue`: the name must be an identifier-named `_tag` `PropertyAssignment`, and its initializer must unwrap to a string-literal-like node. A quoted `"_tag"` key or shorthand `_tag` is not recognized and receives the generic report. Proposed correction: say “An identifier-named `_tag` property assignment with a string-literal value makes the report name that variant.” The rest of the section matches the return/concise-arrow/inside-function variable gates, conditional recursion, logical-right-only traversal, non-empty check, and foreign-return-contract exemption.

### `prefer-effect-schema-guard` — `What it does`

- **What it does: FAIL.** “Anywhere in an `if` condition” is broader than `walkExpressions`. The listener starts at `IfStatement.Expression`, but recursion continues only through children for which `ast.IsExpression(child)` is true. It does not cross non-expression container nodes, such as an arrow block or an object-literal property node, even when such a container holds an `in` expression. The check is otherwise syntactic: it unwraps parentheses, `as`, type assertions, non-null, and `satisfies`; requires binary `in`; and requires the unwrapped left side to be a string or no-substitution template. It performs no symbol or type check. **Proposed correction:** replace “anywhere” with “while descending expression-classified children of an `if` condition,” and state that the walk does not enter non-expression containers.

### `prefer-effect-schema-record` — `What it does`

- **What it does: FAIL.** “Every tuple type alias” is false. `tupleType` reports only when the alias target becomes a tuple after removing a chain of parentheses and/or `readonly`; it does not find tuples nested in unions, intersections, conditionals, object members, or other type forms. The object-shape description is faithful: the rule scans non-`node_modules` files whose extension is exactly `.ts`, walks their full AST, indexes symbols from contextual object-literal types (splitting a top-level union or intersection), then reports interfaces and direct object-type aliases whose declaration symbol is indexed. **Proposed correction:** say “Reports a type alias whose entire target is a tuple, optionally wrapped in parentheses and/or `readonly`,” not “every tuple type alias.”

### `prefer-eta-reduction` — `What it does`

- **What it does: FAIL.** “Used exactly once” reads as a binding-aware condition, but `count` is textual: it counts every identifier node with the same spelling through `ForEachChildAndJSDoc`, without checking that it resolves to the parameter. This creates name-collision false-negative boundaries in callees, property names, nested scopes, type syntax, or JSDoc. Each tower level only requires one syntactic argument; it does not verify a unary call signature. The method exemption is narrower and semantic: only a dot-property callee whose property symbol has `SymbolFlagsMethod` stops the tower. **Proposed correction:** say the parameter spelling must occur exactly once in the body and nowhere in any callee, that calls are syntactically single-argument, and that only checker-identified dot-property methods are exempt.

### `prefer-function-composition` — `What it does`

- **What it does: FAIL.** Two claims hide material syntactic boundaries. For the ordinary block, “non-function initializer” is checked only on the initializer’s immediate AST kind; the code does not unwrap it, so a parenthesized/asserted arrow or function expression can pass this gate. Its “uses it once” test is also identifier-spelling based, not symbol based; `pipe` is recognized only as the bare text `pipe`, with no import resolution. The Effect pipeline similarly uses textual identifier counts, `TypeToString` substring `Effect<`, and exact textual `Effect.runPromise`, with no Effect symbol/import certainty. It does not prove semantic threading. The adapter is also syntactic: one identifier parameter with a type node; bare textual partial callee; non-optional dot property on a same-spelled identifier; no symbol checks. **Proposed correction:** describe these as textual/syntactic shapes. In particular, change “non-function initializer” to “an initializer whose immediate node is not an arrow/function expression,” “exact single-use threading” to “same-spelled identifier counts,” and state that `pipe`, `Effect.runPromise`, the partial callee, and the projected parameter are not resolved to symbols.

### `prefer-function-flip` — `What it does`

- **What it does: FAIL.** The implementation is broader than the displayed `f(x)(fixed)` shape and narrower than “method calls are allowed.” The inner callee need not be a bare identifier and is never symbol-resolved; call expressions and element accesses can also match. Only an unwrapped dot-property inner callee is exempt, so `obj["method"](x)(fixed)` can report. All use/exclusion checks compare identifier spelling, not bindings. **Proposed correction:** describe the shape as `<non-dot-property callee>(parameter)(fixed)`, say the two calls each have one syntactic argument, say same-spelled identifier counts enforce the parameter/fixed gates, and replace “method calls” with “dot-property callees.”

### `prefer-hash-map` — `What it does`

- **What it does: FAIL.** The external-call exemption is stated too broadly. A call is eligible only when its callee is an unwrapped identifier or direct dot-property access, its resolved (and alias-resolved) symbol has at least one declaration, and every declaration is outside the current source file. Parenthesized callees and element-access callees are not eligible. A variable escape is recognized only when `new Map` is the direct initializer of an identifier variable; wrapping the construction before assignment prevents the later-use scan. Direct arguments do allow only parentheses, `as`, `satisfies`, and non-null wrappers. Other documented checks are accurate: global `Map`/`ReadonlyMap` are symbol-resolved; ambient type references are exempt; mutable `Effect.MutableHashMap` is merely textual; `effect` named-import recognition is syntactic; any import declaration from exact `effect/MutableHashMap` reports. **Proposed correction:** narrow the exemption to those callee forms and to a direct identifier-variable initializer, and state the four wrappers allowed only on a direct call argument.

### `prefer-hash-set` — `What it does`

- **What it does: FAIL.** The external-call exemption is stated too broadly. A call is eligible only when its callee is an unwrapped identifier or direct dot-property access, its resolved (and alias-resolved) symbol has at least one declaration, and every declaration is outside the current source file. Parenthesized callees and element-access callees are not eligible. A variable escape is recognized only when `new Set` is the direct initializer of an identifier variable; wrapping the construction before assignment prevents the later-use scan. Direct arguments do allow only parentheses, `as`, `satisfies`, and non-null wrappers. Other documented checks are accurate: global `Set`/`ReadonlySet` are symbol-resolved; ambient type references are exempt; mutable `Effect.MutableHashSet` is merely textual; `effect` named-import recognition is syntactic; any import declaration from exact `effect/MutableHashSet` reports. **Proposed correction:** narrow the exemption to those callee forms and to a direct identifier-variable initializer, and state the four wrappers allowed only on a direct call argument.

### `prefer-inferred-types` — `What it does`, `When to use it`

- What it does: **FAIL**
  - Source reason: `checkContextual` rejects the arrow if **any** parameter lacks a type node. The doc instead says only “at least one” parameter must be explicitly annotated. It also omits the special call-argument gate: a contextual arrow is skipped when an earlier call argument is an empty array literal. Parameter matching is `TypeToString` equality, while the optional return uses mutual assignability plus `TypeToString` equality. The three listeners are variable declarations, function declarations, and arrow functions; variable-parent arrows never enter the contextual check.
  - Proposed correction: say every parameter must be explicitly annotated and its checker-rendered type text must equal the contextual parameter type; document the earlier-empty-array skip and the distinct return-equivalence check. Keep the exact listener/form gates.
- When to use it: **FAIL**
  - Source reason: it repeats the “at least one” claim and omits the earlier-empty-array exemption. For `const` annotations, a generic resolved call is exempt whenever its signature declaration has any type parameter. Return checks only accept expression bodies or blocks containing exactly one `return` with a result; type predicates are exempt.
  - Proposed correction: replace “at least one” with “every parameter,” add the earlier-empty-array exemption, and state the generic-signature and single-result/type-predicate gates explicitly.

### `prefer-layer-unwrap` — `What it does`, `When to use it`

- What it does: **FAIL**
  - Source reason: recognition is narrower and more heuristic than stated. `effect`, `flatMap`, and `get` are accepted by member-name symbol declarations whose normalized file path contains `/node_modules/effect/`, `/effect/src/`, or `/effect/dist/`; the receiver is not verified as `Layer` or `Context`. Direct and curried calls are accepted, as are `.pipe(...)` and identifier-`pipe(...)` shapes. Those `pipe` names are text-only and unresolved. Matching tags use exact resolved symbol identity. “Effect-produced Layer” is only a case-sensitive `TypeToString` substring check for both `Effect<` and `Layer<`. Several wrapper nodes are stripped. Direct forms accept at least two arguments.
  - Proposed correction: describe the exact direct, curried, and pipe shapes; declaration-path member resolution; unresolved textual pipe checks; wrapper stripping; same-symbol requirement; and type-string substring certainty gate.
- When to use it: **FAIL**
  - Source reason: “It only reports Effect APIs” overstates certainty. The three member names are declaration-path checked, but their receivers are not checked, pipe is textual, and the produced type is recognized by string containment rather than semantic Effect/Layer identity.
  - Proposed correction: call it a declaration-path and type-text heuristic, and state its false-positive boundaries instead of claiming full API identity.

### `prefer-option-match` — `What it does`, `When to use it`

- What it does: **FAIL**
  - Source reason: the guard is not resolved to Effect Option. It must be an exact property-access spelling `Option.isSome` or `Option.isNone`; any local identifier named `Option` qualifies. The call only needs one argument and uses the first, which must be an unwrapped identifier. The selected branch is recursively traversed, including nested/JSDoc children, and `.value` is matched by receiver text, not symbol identity. Element access is not matched. Thus shadowed same-text identifiers can report.
  - Proposed correction: describe it as a text/shape check, name the chosen branch (`isSome` true, `isNone` false), and state the first-argument, raw-identifier, recursive dot-access, and no-symbol-resolution limits.
- When to use it: **FAIL**
  - Source reason: “Other guards and non-identifier inputs” is true but incomplete enough to imply semantic Option checking. Parenthesized inputs, aliases, element access, and resolved-but-renamed Option APIs are missed, while an unrelated textual `Option` and shadowed branch identifier can report.
  - Proposed correction: state these syntactic false-negative and false-positive boundaries.

### `prefer-result-concept-names` — `What it does`, `When to use it`

- What it does: **FAIL**
  - Source reason: “property name” and “inferred name noun” hide finite, material gates. The listeners cover identifier-named function-valued variable declarations, function declarations, and methods. Return discovery recursively crosses nested function/JSDoc children. Eligible dot properties must be non-callees on a parameter-symbol receiver, or be the outer property of `this.x.y`; string element access only works on an identifier receiver. If body source contains `.map(`, any identifier receiver becomes eligible. Only when no eligible return property exists does it scan the whole body. Names use a fixed word regex, a finite `resultOps` set, `from`/`to`, wrapper-noun handling, naive suffix/plural agreement, and a `label` plus textual `format(` exception.
  - Proposed correction: document these exact listener, traversal, receiver, `.map(`, finite-name, agreement, and exception heuristics. Explicitly say nested and non-returned evidence can affect the result.
- When to use it: **FAIL**
  - Source reason: it presents “the single property that the rule finds” without explaining which properties can be found or that `.map(` changes receiver eligibility. This can both miss a semantic result property and count unrelated/nested/JSDoc properties.
  - Proposed correction: scope use to this finite syntactic/symbol heuristic and state the main false-positive and false-negative boundaries.

### `prefer-schema-tagged-struct` — `What it does`

- What it does: **FAIL**
  - Source reason: the blocked-substring list and portability caveat are exact, but “Effect Data.TaggedClass” is stronger than the recognizer. `fromEffectTagged` checks only a property-access member named `TaggedClass` whose symbol has a declaration under `/node_modules/effect/`, or at the exact Effect source/dist suffixes. It never verifies that the receiver is `Data`. It scans every heritage-clause type whose expression is a call and reports the class name (or anonymous class node).
  - Proposed correction: describe the declaration-path/member-name recognizer and lack of receiver verification, while retaining the exact case-sensitive substring list over the full matched heritage type text.

### `prefer-specific-operation-names` — `What it does`, `When to use it`

- What it does: **FAIL**
  - Source reason: “one clear role” is a finite text/AST heuristic. It recursively traverses the whole body, including nested/JSDoc children. It derives roles from fixed first-word callee sets, non-callee property accesses, and plain `=` assignments. It then applies fixed precedence deletions. Conflicting projection properties remove projection evidence. It finds the first vague word among name tokens, exempts any name containing `handler`, `callback`, `listener`, or `subscriber`, and exempts `handle` followed by only one of `click`, `change`, `submit`, `message`, `error`, or `load`. The replacement uses a lowercase substring index, not a preserved parsed noun boundary.
  - Proposed correction: replace “clear role” with the exact finite evidence and precedence algorithm, list the name/event exemptions, and describe the substring replacement behavior.
- When to use it: **FAIL**
  - Source reason: named functions, methods, and identifier-named arrow/function-expression variables are the listener forms, but functions with a unique detected role are still exempt for the fixed handler/callback/listener/subscriber and handle-event names. “Bodies with no unique role” omits these exemptions and the fixed role vocabulary.
  - Proposed correction: state the exact callable forms, finite role vocabulary, whole-body traversal, and exemptions.

### `process-environment` — `What it does`

- What it does: **FAIL**
  - Source reason: the root must be dot `process.env` or string-element `process["env"]`, but later chain elements can be any element access, not only string elements. `process.env[key]` can report. Parentheses, `as`, and `satisfies` wrappers are skipped when finding the outermost access, but non-null wrappers are not. A chain such as `process.env!.KEY` can therefore report both the inner `process.env` and the outer access, contrary to the singular “outermost” claim. The global `process` symbol is required only when `ResolveName` succeeds; if it returns nil, any textual `process` root qualifies.
  - Proposed correction: distinguish the root-name rule from unrestricted later element accesses; document symbol fallback; either exclude non-null chains from the “outermost” claim or describe the possible duplicate reports.

### `raw-fetch-abort-signal` — `What it does`, `When to use it`

- What it does: **FAIL**
  - Source reason: the documented acceptance regex behavior is accurate, but “raw fetch syntax” is not bounded accurately. `hasFetch` applies `(?:\bfetch|(?:globalThis|window|self)\.fetch)\s*\(` to raw whole-call text. Because the first alternative is unqualified, it also matches `client.fetch(`, `$fetch(` from the `fetch` substring, and matching text in comments or strings. The parameter regex is also applied to the whole call and can extract the first matching identifier-shaped text, not necessarily an actual callback parameter. There is no symbol/import resolution.
  - Proposed correction: include both exact regexes and say they scan unparsed whole-call text, can match qualified/custom/comment/string text, and can misidentify a non-parameter token. Retain the existing warning that an accepted signal occurrence need not be fetch init.
- When to use it: **FAIL**
  - Source reason: calling the target “raw `fetch`” implies a narrower check than the regex implements. The check can prompt on unrelated textual matches and accept unrelated brace-delimited signal text.
  - Proposed correction: describe it as a broad whole-call regex prompt with those false-positive and false-negative boundaries, not as proof of raw fetch or cancellation plumbing.

### `raw-fetch-outside-adapter` — `What it does`, `When to use it`

- What it does: **FAIL**
  - Source reason: raw fetch recognition is exact, unresolved callee text: `fetch`, `globalThis.fetch`, `window.fetch`, or `self.fetch`. Adapter exemption is an exact, case-sensitive path segment `adapter` or `adapters`. The `Effect.tryPromise`/`tryPromise` exemption is not general containment: `insideTryPromise` walks call ancestors but stops at a function-like ancestor unless that function is the fetch call's immediate parent. Therefore the documented expression-body example is exempt, but a normal block callback such as `Effect.tryPromise(() => { fetch("/ok") })` is reported. A wrapped or nested expression can also stop before reaching `tryPromise`. Conversely, a fetch passed directly inside the textual call can be exempt without callback semantics. No API symbol is resolved.
  - Proposed correction: state the four exact textual fetch forms, exact path segments, and the precise ancestor/function-boundary algorithm. Do not claim all fetches “in Effect.tryPromise” are allowed.
- When to use it: **FAIL**
  - Source reason: the boundary advice omits the narrow and shape-sensitive `tryPromise` exemption and the case-sensitive exact adapter path gate, which materially changes normal block-bodied callback behavior.
  - Proposed correction: state those exact boundary shapes and false-positive/false-negative limits.

### `redundant-alias` — `What it does`

**What it does failure:** The alias description makes the optional utility wrapper sound syntactic. The regexp actually permits each of `Omit`, `Partial`, `Pick`, `Readonly`, and `Required` as a bare prefix and makes `<` optional. For example, the leading text `PartialCustomer` can be treated as wrapper `Partial` plus captured target `Customer`. It also captures identifier-shaped keywords without symbol or import resolution. The interface claim omits the shared `report` exemption: a self-named empty interface such as `interface X extends X {}` is not reported.

**Proposed correction:** Say that the rule listens only to interface and type-alias declarations. An interface needs a name, a non-nil empty member list, exactly one heritage clause, and exactly one heritage type; the full trimmed heritage text is the target. An alias uses the raw leading-text regexp, whose utility prefix and `<` are both optional, captures the next ASCII identifier-shaped token, and ignores all trailing syntax. Both paths report only when the textual target is nonempty and differs from the declared name. State that there is no checker, symbol, or import resolution.

### `require-because-in-comments` — `What it does`

**What it does failure:** This is not an AST/trivia comment check. At `EndOfFile`, a custom byte scanner finds `//` and `/*...*/`. It skips single- and double-quoted strings and template raw text, and scans template substitutions. It does not parse regular-expression literals, so comment-looking text in a valid regexp, such as the `//` inside `/[//]/`, can be reported as a comment. Unterminated block-comment text is also included. The doc's unqualified “line and block comments” hides this false-positive boundary.

**Proposed correction:** Describe the custom whole-file text scan and its string/template exclusions. State that `because` is a case-insensitive standalone Unicode word. Warn that regexp literals are not recognized, so comment delimiters inside them can be treated as comments. No checker, symbol, or import information is used.

### `require-callable-role-name-consistency` — `What it does`

**What it does failure:** “Against their signature or body shape” hides materially weak and asymmetric checks. `reducer` only requires two parameters; it never checks a result or accumulator compatibility. An unannotated `mapper` has `unknown` shape and passes as non-void. `callback` and `function` require the *return annotation* to contain `=>`, so they mean a callable result, not merely a callable declaration. `accessor` accepts only a narrow textual property-return regex. All result shapes are substring classifications with priority (`=>`, boolean/type-predicate, number, void, keyed, collection, string, other annotation, unknown), not checker types.

**Proposed correction:** Keep the documented three identifier-named listener forms and two-word/end-role gate, then give the exact finite contracts: `predicate` boolean/type-predicate text; `mapper` at least one parameter and shape other than void; `reducer` at least two parameters only; `comparator` number text; `factory` crude object/`return {`/`new ` body evidence; `handler` void shape or `effect` text; `accessor` the narrow `return? word.word` body form; `callback`/`function` a return annotation containing `=>`. State that inferred types, symbols, imports, assignability, and body control flow are not resolved.

### `require-command-name-consistency` — `What it does`

**What it does failure:** “Return-type text to contain void” is not exact. The shared shape classifier gives `=>`, boolean/type-predicate, and number priority over `void`; for example, a return annotation `() => void` has `callable` shape and a `save...` name reports even though its text contains `void`. `effect` is a separate unrestricted substring exemption. Despite the diagnostic saying “result and body,” the body is never consulted. The inverse check is also broader than “many accessors or results”: almost every explicit-void callable whose first word is not `publish|save|send|write` reports, except multiword predicate-like first words and names ending `handler|callback`; single-word names still report.

**Proposed correction:** State the exact identifier-named arrow/function-expression variable, function declaration, and method listeners. Define the four command prefixes, the prioritized textual shape check, and the independent `effect` substring exemption. Define the inverse void gate and its finite accessor words (`find|get|load|lookup|read|select|create|resolve`), single-word branch, predicate-word exemption, and `handler|callback` suffix exemption. State that inference, body effects, symbols, imports, and checker types are not inspected.

### `require-construction-name-consistency` — `What it does`

**What it does failure:** The doc describes semantic construction by a callable, but the rule covers only identifier-named arrow/function-expression variables, function declarations, and methods. Construction is crude syntax/text evidence: direct object-literal body, body starting `({`, body containing `return {`, or body containing `new ` anywhere. The missing-factory-vocabulary branch also needs the claimed noun to equal a concept heuristically extracted from an explicit return annotation; inferred results do not qualify. Conversely, the factory/lookup branch needs no return concept and reports only when lookup text exists *and* none of the crude construction evidence exists.

**Proposed correction:** State those listener and identifier gates. List factory first words `build|construct|create|make` and variant first words `fail|left|none|of|right|some|succeed`. Describe the construction substrings, `.find(` or `word[...]` lookup text, first/claimed-result word parsing, and last-identifier return-concept heuristic. State that the rule does not resolve constructors, freshness, aliases, imports, symbols, return inference, or control flow.

### `require-lookup-totality-name-consistency` — `What it does`

**What it does failure:** “Annotated return is one total value” and “return is optional” sound semantic. Cardinality is only lowercase substring matching. Keyed and collection text are classified before optional text. Optional means text containing `undefined`, `null`, `option.`, or `result.`; ordinary generic spellings such as `Option<User>` and `Result<User, E>` do not contain the required dot and are classified as one, while unrelated identifiers containing `null` or `undefined` can be classified optional. Inferred returns are unknown and never report. Only the three shared identifier-named callable forms are visited.

**Proposed correction:** Describe the textual cardinality order and exact substrings. State that optional first words are exactly `find|lookup|maybe|optional`, total first words are `require|unsafe`, and `get or throw|else` needs the first three parsed words. State that there is no type inference, alias expansion, generic `Option`/`Result` recognition, symbol/import resolution, or body/throw analysis.

### `require-predicate-name-consistency` — `What it does`

**What it does failure:** The claim that predicate names accept return-type text “containing boolean” misses classifier priority. A type such as `() => boolean` is `callable`, not boolean, because `=>` is checked first; boolean/type-predicate text also outranks number and void. Predicate-style names are a finite first-word list, plus two-word `starts with`/`ends with`; `match`/`matches` are not predicate claims despite appearing in the diagnostic help. Single-word `every` and `some` are exempt. The incompatible-operation side is another finite list, not all incompatible operations. All checks use only explicit annotation text.

**Proposed correction:** List the exact predicate first words (`can|contain|contains|does|equal|equals|every|exist|exists|has|include|includes|is|should|some`), the `starts with`/`ends with` forms, and the single-word exemptions. List or clearly delimit the exact incompatible first-word set. Describe prioritized substring shape classification and the three identifier-named listener forms. State that inferred body results, type aliases, checker types, symbols, and imports are not inspected.

### `require-result-cardinality-name-consistency` — `What it does`

**What it does failure:** The broad claim that it checks a result noun against annotated cardinality hides an important one-way boundary. Many/keyed annotations with a singular non-neutral claimed noun report. But a plural claimed noun with `one` or `optional-one` reports only when the separate return shape is *not* `object`; any ordinary named object annotation such as `User` has object shape, so a plural name can pass. Cardinality, shape, noun choice, and plurality are all text heuristics, not resolved types or grammar-aware inflection.

**Proposed correction:** State the three supported identifier-named callable forms and explicit-annotation requirement. Explain that the claimed result is the word before the first `from|to|by`, otherwise the last word. Define textual keyed/many/optional/one classification. State the asymmetric plural-single gate, the exact neutral nouns (`advice|config|data|evidence|metadata|news|series|species|status`), and the suffix/`children`/`people` plural heuristic. State that aliases, imported collection types, checker cardinality, symbols, and body inference are not used.

### `schema-error-class` — `What it does`

- What it does: **FAIL**
  - Reason: The claimed “recognized Schema error class” exemption is not symbol- or import-resolved. Production exempts when the class source both matches a textual `extends` regex for `TaggedErrorClass`, `ErrorClass`, or `TaggedError` with at most one arbitrary qualifier and contains `Schema` anywhere. Thus an unrelated qualifier can be exempt if `Schema` occurs elsewhere, while an aliased genuine Schema base can fail the exemption. `Data`-style bases and `Error` qualification are also textual regex checks.
  - Correction: Describe the check as class-source regex matching, give the exact accepted base spellings, and state that it does not resolve imports or prove Schema provenance.

### `schema-optional-key` — `What it does`

- What it does: **FAIL**
  - Reason: The document omits the call-recognition gate. Production first parses import text with a regex. It recognizes namespace members imported from `effect`, namespace imports from `effect/<family>`, and direct/aliased members from `effect/<family>`. It does not resolve call-site symbols and can misclassify a shadowed imported local. Default imports, re-exports, and other spellings are not recognized. The property assignment can be anywhere in the file; it need not be inside `Schema.Struct`.
  - Correction: State the accepted import/call forms, the textual rather than semantic resolution, the shadowing boundary, and that every property assignment is eligible after the file-wide optional-signature name scan.

### `scoped-background-work` — `What it does`

- What it does: **FAIL**
  - Reason: “FiberSet, or FiberMap” and “scoped acquisition” overstate the exemption. Production only accepts ancestor calls named `Layer.effect`, `effectDiscard`, `scoped`, or `scopedDiscard`; `Effect.forkScoped` or `forkIn`; `FiberSet.make`, `makeWith`, `run`, `add`, or `makeRuntime`; and `FiberMap.make`, `makeWith`, `run`, `set`, or `makeRuntime`. Ownership is only syntactic ancestry, not data-flow or argument ownership. API recognition comes from a regex-parsed import-name table rather than call-site symbol resolution, so shadowing can cause false positives. A `Stream` candidate under `Effect.forever` can report in addition to the enclosing `forever` call.
  - Correction: List the finite ancestor names and say that any matching syntactic call ancestor exempts the candidate. Document the textual import forms, shadowing limit, and possible separate reports for the stream call and `forever`.

### `scoped-client-cache` — `What it does`

- What it does: **FAIL**
  - Reason: The finite method lists and whole-`Cache.make` subtree boundary are accurate, but “imported” implies stronger resolution than production provides. Imports are parsed textually with a regex. Calls are matched by stored local names, including aliases and direct members from `effect/<family>`, without resolving the call-site symbol. A shadowed imported name can therefore report, while default imports, re-exports, and unsupported spellings are missed.
  - Correction: Add the accepted textual import forms and explicitly state the lack of call-site symbol resolution and its shadowing/unsupported-import boundaries.

### `service-method-effect-fn` — `What it does`

- What it does: **FAIL**
  - Reason: “Public” and “wrapped” are too strong. Class checking has no accessibility gate: every method/property/shorthand found recursively under any class whose raw source contains `Context.Service` can qualify, including private members and declarations nested inside the class subtree. Any descendant named `Effect.fn` with a first string-literal argument exempts the whole value; it need not wrap the operation. Any descendant `Effect.gen` also exempts it. These API checks use regex-parsed import local names, not call-site symbols, so shadowing is unresolved. Export checks only use syntactic `export` modifiers and do not follow export lists.
  - Correction: State the exact syntactic export and raw-class-text entry gates, lack of accessibility filtering, recursive descendant exemptions, and textual import-name resolution. Replace “wrapped” with “contains a recognized named `Effect.fn` call.”

### `speculative-export` — `What it does`

- What it does: **FAIL**
  - Reason: The rule does not establish a consumer, reference, first-party boundary, or symbol relationship. For ordinary ASCII names it accepts any same text word in any other non-declaration Program source whose path lacks `/node_modules/`; comments, strings, and unrelated declarations count. Non-ASCII or `$` names use a separate regex-word-boundary path. The entry gate is a syntactic `export` modifier, so a local declaration exported only through an export list is not checked.
  - Correction: Describe this as a cross-file raw-text occurrence heuristic, define the source-file filters and ASCII/fallback tokenization, state that comments and unrelated text suppress reports, and state that export lists are outside the listener gate.

### `stream-pagination` — `What it does`

- **What it does: FAIL.** The summary names the three loop kinds and broad signals, but omits material detection boundaries. The listener covers only `WhileStatement`, `DoStatement`, and classic `ForStatement`, not `for...in` or `for...of`. Each full loop subtree is recursively searched, including nested functions. A token is any identifier or string-literal-like node whose text contains one of the case-insensitive regex alternatives `pageToken`, `nextPageToken`, `nextCursor`, `cursor`, `continuation`, `pageKey`, or `offset`; it need not be the loop condition or be related to the accumulation. Accumulation is any `YieldExpression`, or a property-access call ending exactly in `push`, `concat`, `append`, `appendAll`, or `yield`; direct and element-access calls do not count. The exemption scans the whole nearest enclosing function-like subtree for a recognized `Stream.paginate`, even if unrelated or nested. Recognition is textual, not checker-backed: it accepts named root imports such as `{ Stream as S } from "effect"`, namespace imports from an `effect/<family>` path, and direct named members from such a subpath; unsupported forms are missed and shadowed locals can still match.
  - **Proposed correction:** Add these listener, recursive-traversal, finite-name, non-correlation, nearest-function exemption, and textual-import-resolution limits to this section.

### `test-clock-for-time` — `What it does`

- **What it does: FAIL.** The implementation is narrower and more textual than “inside tests” suggests. It listens to every call expression, but considers a call inside a test only when it is a lexical descendant of a call to a named `it` import (alias allowed) from exactly `@effect/vitest` or `@effect/vitest/index`. Both bare `it(...)` and any direct property call `it.<name>(...)` qualify; the property is not limited to `effect`. It does not follow calls into helpers declared outside that subtree. The exact Effect names are `sleep`, `timeout`, `timeoutTo`, `timeoutFail`, `timeoutFailCause`, `retry`, `retryOrElse`, and `retryOrElseEither`. The exact Schedule names are `exponential`, `fibonacci`, `spaced`, `linear`, `delayed`, `delayedEffect`, `addDelay`, `addDelayEffect`, `jittered`, and `jitteredWith`. Effect/Schedule API identity uses the same textual import parsing described above, not symbol identity, so unsupported imports can be missed and shadowing can false-positive. The file exemption is also only `(?m)import\s+.*\bTestClock\b`: any same-line import text containing `TestClock`, even from an unrelated module or side-effect import, disables the whole file, while a multiline import can fail to disable it.
  - **Proposed correction:** Enumerate the exact call names and document the lexical `it` ancestor gate, accepted `it` forms, no call-graph traversal, textual Effect/Schedule imports, shadowing boundary, and exact file-wide `TestClock` regex exemption.

### `test-sleeps` — `What it does`

- **What it does: FAIL.** The rule recognizes only a lexical descendant of a call to named `it` (alias allowed) imported from exactly `@effect/vitest` or `@effect/vitest/index`. Bare `it(...)` and every direct `it.<name>(...)` call qualify, not only `it.effect(...)`. A helper defined outside the test subtree is not followed; nested syntax inside the subtree is. `Effect.sleep` identity is based on the textual import parser, not checker symbols: supported root named family imports and supported `effect/<family>` namespace/direct-member imports match, unsupported forms are missed, and a shadowing local can false-positive. The docs do not state these entry gates and false-positive/false-negative boundaries.
  - **Proposed correction:** State the exact `CallExpression` listener, lexical ancestor forms, exact vitest modules, no helper call-graph traversal, and textual `Effect.sleep` import resolution with its shadowing limit.

### `typed-boundary-error` — `What it does`

- **What it does: FAIL.** The four API names are correct, but “throw or construct `Error`” hides important textual and traversal behavior. For a call with two or more arguments the rule scans only argument 2; with one argument it scans argument 1; with no arguments it does nothing. It recursively scans the entire selected subtree without a nested-function boundary. Any `ThrowStatement` qualifies regardless of what is thrown, including a typed domain error. Construction qualifies only for `new` whose callee is an identifier or property access with terminal text exactly `Error`; `Error(...)`, subclasses, and semantic aliases do not qualify, while a shadowed local or unrelated `X.Error` does. Catch API identity is textual import parsing rather than checker resolution, with unsupported-import false negatives and shadowing false positives.
  - **Proposed correction:** Document argument selection, unrestricted recursive handler traversal, “any throw” behavior, exact textual `new Error` terminal-name check, and textual Effect import resolution. Replace wording that implies semantic built-in `Error` recognition.

### `typed-error-recovery` — `What it does`

- **What it does: FAIL.** The docs overstate semantic certainty. The listener recognizes only textually imported Effect/Stream `catchCause` and `catchAllCause` calls. It chooses argument 1 as the recovered value only when that argument exists and is not an arrow/function expression after transparent wrappers. Otherwise it recognizes only an immediate outer `self.pipe(...)` call or a bare immediate outer identifier call literally named `pipe(self, ...)`; this pipe recognition is not symbol-backed. It then converts the chosen expression's checker type to text and applies `(?:Effect|Stream)<\s*[^,>]+,\s*([^,>]+)`. It reports only if this limited rendering matches and the captured second type argument is not exactly trimmed `never`. Unresolved, differently rendered, qualified, or nested-generic types can therefore be silently allowed, as can unsupported call/pipe shapes. Conversely textual API/pipe shadowing can match incorrectly.
  - **Proposed correction:** Describe the exact data-first/immediate-pipe source selection, the TypeToString regex certainty gate and nested-rendering limits, exact `never` comparison, and textual import/pipe identity. Say that `never` is one allowed case, not the only reason a call may be unreported.

### `unbounded-stream-buffer` — `What it does`

- **What it does: FAIL.** The core literal condition is correct, but the source boundaries are absent. A textually recognized `Stream.buffer` call is checked across **all** arguments. After transparent expression wrappers, only direct object literals are inspected. Within them, only direct `PropertyAssignment` entries whose statically extractable identifier, literal, no-substitution-template, or literal-computed name is `capacity` qualify. Shorthand, spread-derived, variable-held, dynamically computed, and nested options are not followed. The initializer is unwrapped through transparent expressions and must itself be string-literal-like with exact case-sensitive text `unbounded`; constants and semantic values are not resolved. Stream API recognition is textual rather than checker-backed, so unsupported imports are missed and shadowing can false-positive.
  - **Proposed correction:** Add the all-arguments/direct-object/direct-property scope, exact syntactic name/value rules, no constant/spread/variable resolution, transparent wrappers, and textual Stream import boundary.

### `unbounded-stream-collect` — `What it does`

- **What it does: FAIL.** “Every Effect `Stream.runCollect` call” omits that identity is textual and import-shape-limited. The sole `CallExpression` listener matches a property access on a root named family import such as `{ Stream as S } from "effect"`, a namespace import from an `effect/<family>` path, or a direct named `runCollect` import from such a subpath, with transparent callee wrappers. It does not use checker symbols, so unsupported import/re-export shapes are missed and a matching imported name shadowed locally can false-positive. There is no production-file, test-file, boundedness, source-shape, or call-context exemption.
  - **Proposed correction:** State the accepted textual import/callee forms and shadowing boundary, and explicitly state that every recognized call in every file/context is reported, irrespective of boundedness or “production” status.

### `unused-field` — `What it does`

- **What it does: FAIL.** The high-level scope is mostly correct, but “read,” boundary use, and support-field exemptions need exact limits. The rule makes one whole-program recursive pass over every non-declaration source file. It counts any non-declaration **identifier** whose checker symbol is the field symbol; it does not distinguish reads from writes, and non-identifier access forms are not generally counted. It excludes only the exact mechanical-forwarding shape `{ field: value.field }` where both names are the same identifier. Independently, any textual `Struct.get("field")` property-access call makes that field name read globally, without checking the `Struct` symbol, receiver import, owning type, or later arguments; aliases, element access, and nonliteral first arguments do not count. Export-boundary use is based on identifier symbols found by the same traversal and has nesting cutoffs: a directly encountered exported function declaration or exported variable statement can qualify, while a method, nested non-exported function, or nested variable declaration can stop qualification. Only declarations of a field in the current source file are report targets, although checker properties include merged/inherited type properties. The exact ignored names are prefixes `__` and `~effect/`, plus `pipe`, `toJSON`, `toString`, and `[TypeId]`; any field with a method, method-signature, getter, or setter declaration is also ignored. Direct function-, constructor-, and union-type aliases alone are skipped; interfaces and all other identifier-named aliases are entered. Computed report names use a statically extractable property name or the trimmed computed-expression source text.
  - **Proposed correction:** Replace the broad “independent read” and “built-in support fields” wording with these exact identifier-symbol, write/forwarding, textual `Struct.get`, whole-program/declaration-file, export nesting, current-file target, finite ignored-name, method/accessor, type-alias entry, and computed-name rules.

## Blockers

- Zero-failure acceptance is blocked by the 91 failed judgments above across 81 pages.
- No catalog, row-accounting, file-access, source-inventory, input-stability, fixture, or test-command blocker occurred.
