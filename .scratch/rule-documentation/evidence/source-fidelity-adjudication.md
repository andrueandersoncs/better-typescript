# DOC-14 third-source finding adjudication

**Status: COMPLETE**

## Scope and standard

- Adjudicated all 91 failed judgments in `.scratch/rule-documentation/evidence/source-fidelity.md`.
- Read each affected page with its production rule, test, and fixtures.
- Applied the authoritative request and Shared page contract.
- `CONFIRMED` means an existing claim is materially false, contradictory, or over-broad, usage advice promises an unsupported guarantee, or an example has the wrong verdict.
- `REJECTED` means the finding only asks for omitted implementation detail and the existing concise claim remains true.
- Omission alone is not a failure. No page, project record, or prior evidence was edited.

## Findings

| Slug | Axis | Classification | Reasoning | Exact correction |
| --- | --- | --- | --- | --- |
| `boundary-schema-decode` | What it does | REJECTED | The page says boundary receiver names “include” the listed terms and only promises a recognized decode call somewhere in the surrounding function. The implementation’s substring receiver test, recursive scan, ancestor exemption, and lack of symbol/data-flow resolution are implementation limits; they do not contradict that wording or either example. | — |
| `bounded-retry-schedule` | What it does | CONFIRMED | “Whose retry policy is an unbounded schedule” is a semantic claim the code does not establish. `retryPolicy` can select a sole non-function argument, and `policyIsBounded` uses raw bound words; an object with no `schedule` is allowed by default. Thus reporting is a textual heuristic, not a determination that a schedule is unbounded. | Replace the first sentence with: “Reports bare `retry(...)` and exact-identifier `Effect.retry(...)` calls when the syntactically selected policy fails a textual bound heuristic. An object first argument is the policy; otherwise argument 2 is used when present, or a sole non-function argument is used. A non-object is allowed when its raw text contains a listed bound word. An object is allowed by a numeric/identifier `times`, any `while` or `until`, a bounded `schedule`, or no `schedule`.” |
| `cache-per-request` | What it does | REJECTED | The page already states the receiver-text suffix, parameter/non-module-scope rule, module-scope zero-parameter allowance, and ancestor cache-construction allowance. The exact recognized function forms, nearest-function implementation, and cross-function ancestor behavior are omitted edge details, not contradictions. | — |
| `cache-preference` | What it does | REJECTED | The documented `new Map()` parent/binding cases and file-wide `Cache.make`/`makeWith` exemption match the code. The finite TTL-name set, acceptance of bare `set(...)`, and lack of symbol resolution add syntactic edges but do not make the described `new Map()` and `.set()` cases false. | — |
| `closed-abstraction` | What it does | REJECTED | The page expressly describes raw-substring type ownership and project-wide lexical word counting minus the declaration occurrence. The omitted named-function collection details, file exclusions, and comment/string false positives refine that already-textual heuristic; they do not contradict it. | — |
| `config-refined-values` | What it does | REJECTED | The page gives the exact key suffixes and exact ancestor-call names used by `refinedKey` and `refinedCalls`. Receiver suffix matching, suffix boundary behavior, arbitrary ancestor receivers, and no symbol resolution are parser/alias boundaries; the page does not promise semantic Effect import resolution. | — |
| `dependent-layer-merge` | What it does | REJECTED | The page accurately states the rendered-`Layer`, object-reference, three-type-argument, channel-certainty, and assignability tests. Receiver entry syntax, one-level array flattening, ordered pair iteration, and direct flag implementation are omitted traversal/certainty details, not a false existing guarantee. | — |
| `duplicate-shape` | What it does | CONFIRMED | “Reports an interface or type alias whose normalized source text matches another” overstates which matching declarations report. `inspect` selects the smallest `filename:name` key and suppresses that declaration; the fixture confirms only `UserRow` reports while matching `UserRecord` is silent. | Replace the first sentence with: “Groups project interfaces and type aliases by normalized source shape. For each shape, the lexicographically smallest `filename:name` key is silent; a matching declaration reports only when its key differs from that selected key. Declarations sharing the selected key are also silent.” |
| `effect-fn-name` | What it does | CONFIRMED | “Reports `Effect.fn` wrappers without” a valid name covers non-invoked builders, but `Effect.fn("load")` is not reported unless that builder call is immediately invoked. The separate listener branch reports a direct arrow/function/object argument as unnamed even without an outer invocation. | Replace the opening with: “Reports a missing or invalid name on an `Effect.fn(...)` builder only when that builder is immediately invoked. Separately, it reports a direct arrow function, function expression, or object-literal first argument as unnamed even without an outer invocation. Matching is textual on property-access receivers ending in `Effect`; a string is accepted when it begins with two non-empty dot-separated parts.” |
| `effect-test-style` | What it does | CONFIRMED | “When its callback return type contains `Effect` or its callback uses `Effect.`” is over-broad because the listener only inspects an inline arrow/function argument. A callback identifier returning Effect is ignored, despite still being the test’s callback. | Replace the first sentence with: “Reports the listed plain `it` call forms only when the call contains an inline arrow-function or function-expression callback—the rightmost such argument—whose rendered return type contains `Effect` or whose raw callback text contains `Effect.`. The file gate is a raw `@effect/vitest` substring.” |
| `function-derived-model` | What it does | REJECTED | The page accurately states the implemented stem/suffix/raw-source condition and only quotes the diagnostic text containing “sole function role.” The finding asks for project traversal, symbol-resolution, and reference-counting limits; the page does not claim that the implementation proves sole use, so no existing enforced-behavior claim or example verdict is false. | — |
| `http-status-decode-order` | What it does | CONFIRMED | “HTTP body decoding before status classification” implies HTTP and ordering semantics that the rule does not establish. Any call with a listed final name can report, regardless of receiver, and “before” is recursive AST visitation order; unrelated earlier `status`, `ok`, `statusText`, or classifier names suppress it. The code also checks seven decoder names omitted from the behavior statement. | Replace the first paragraph with: “In the nearest enclosing function, reports calls ending in `json`, `text`, `arrayBuffer`, `blob`, `formData`, or `bytes` when no earlier recursive AST visit sees a property named `status`, `ok`, or `statusText`, or a call named `filterStatusOk`, `filterStatus`, or `matchStatus`. It applies the same check to `decodeUnknown`, `decodeUnknownEffect`, `decode`, `decodeEffect`, `schemaBodyJson`, `schemaJson`, and `schemaNoBody` only when the function also contains a listed body/classifier call or `execute`, `get`, `post`, `put`, `patch`, or `del`. Names and order are syntactic; receivers, symbols, data flow, and control flow are not resolved.” |
| `idempotent-retry` | What it does | REJECTED | “Such as `save`” and “such as `fetch`” do not claim exhaustive verb sets, and the page does not say the rule proves idempotency. The hand-built import forms, nearest-function naming forms, anonymous boundary, and shadowing behavior are finite listener/alias details that do not falsify the stated mutation-prefix behavior or examples. | — |
| `layer-forever-acquisition` | What it does | REJECTED | The page names the exact acquisition APIs, forever/run combinations, run methods, and `forkScoped` exemption implemented by the rule. The top-level import table, argument positions implied by each API, unlimited descendant scan, and shadowing limits are incidental import/traversal details, not contradictory guarantees. | — |
| `missing-rationale` | What it does | REJECTED | The page already limits the rule to “supported top-level” declarations and accurately describes the previous-statement comment window and `because` substring. Exact structural predicates and raw class/type-text heuristics refine “supported”; omission does not claim every semantic data class or Schema value is recognized. `RequestData` is the exact diagnostic for the shown fixture/example, not a claim that every name is static. | — |
| `no-blank-lines-between-single-line-declarations` | What it does | CONFIRMED | The page/report says blank lines “remain required around multi-line declarations,” but the implementation returns without checking whenever either declaration is multi-line. The page later says those pairs are not reported, creating a direct contradiction and an unsupported enforced requirement. | After the exact diagnostic quote, add: “Despite that help text, this rule does not enforce separators around multi-line declarations; any pair with a multi-line declaration is exempt.” |
| `no-callbacks` | What it does | REJECTED | The page says “selected function-type annotations,” so it does not promise every annotation placement. Its positive claim—non-ambient listed callable forms with `void` return and a callable parameter—matches the signature/type checks, and it correctly excludes accessors and constructors. Wrapper, rest-parameter, and missing-type certainty gates are incidental limits. | — |
| `no-duplicate-function-names` | What it does | REJECTED | The core contract is accurate: top-level same-name function declarations or function-valued variables, different files, and mutual assignability. Direct/unwrapped initializer syntax, declaration-file/node_modules exclusions, same-file omission, and whole-value comparison are finite scan/type details; no body comparison is promised in this paragraph. | — |
| `no-duplicate-function-names` | When to use it | CONFIRMED | “Find semantic copies” promises more than same name plus mutually assignable callable types. `checkSourceFile` never compares bodies or behavior; the fixture functions happen to match, but the rule would also report different implementations with the same name/type. | Replace the section with: “Use it to find same-name, mutually assignable top-level callables in different files as candidates for consolidation. The rule does not compare bodies or establish semantic equivalence. Types that are not mutually assignable in both directions are allowed.” |
| `no-duplicate-if-bodies` | What it does | REJECTED | The page already states immediate consecutive statements in one block, the later-body exit gate, the top-level allowance, and the separate `else if` comparison. Token trivia/semicolon removal and one-statement-block normalization are the implementation’s reasonable fingerprint for “identical,” not a contradiction of the behavior claim. | — |
| `no-error-type` | What it does | REJECTED | The page’s “type references that resolve to the built-in `Error` type” describes the written `Error` reference and built-in-symbol check used by the rule. Expansion through a differently named alias is an unclaimed alias-resolution detail. Its omission does not make the direct built-in `Error` claim or either example verdict false. | — |
| `no-explicit-any-return` | What it does | REJECTED | “Explicit function return types that contain `any`” naturally describes the syntactic `AnyKeyword` recursion used by the rule, and `Promise<any>` is correctly covered. It does not claim inferred returns or differently named aliases are expanded. The finite listener set and independent nested reports are implementation details that do not falsify the statement or examples. | — |
| `no-first-party-schema-declare` | What it does | REJECTED | The page already limits the claim to bare `Schema.declare`, the first predicate call signature, the asserted-type gates, and qualifying declaration kinds. Text matching, declaration paths, merged symbols, and direct-intersection mechanics are incidental certainty details; their omission does not falsify that summary or either example. | — |
| `no-immediate-effect-sync` | What it does | CONFIRMED | The page promises, “A local lookalike is allowed.” `effectCallMatcher` instead records imported local spellings and later compares only callee text, so a nested declaration shadowing a recorded spelling can still report. | Replace that sentence with: “A local lookalike is allowed when its spelling was not registered by a recognized import. A declaration that shadows a registered import spelling can still match because callee matching is textual.” |
| `no-inline-closures` | What it does | REJECTED | The finding asks for the internal wrapper traversal used to decide whether an arrow reaches an external call. That is an incidental parser/traversal boundary, not a contradiction of the documented normal direct-callback exemption or the tested verdicts. | — |
| `no-manual-type-dispatch` | What it does | REJECTED | “Shares an identifier” remains true when sharing means an identifier spelling. The page does not promise checker-symbol identity; the text-versus-symbol distinction and inclusion of property-name nodes are incidental matching details. | — |
| `no-mutable-array-methods` | What it does | REJECTED | The named nine methods and array-like receiver behavior are accurate. Omission of computed element-access syntax and method-symbol resolution is an incidental listener/parser boundary, and no documented example verdict is wrong. | — |
| `no-mutation` | What it does | REJECTED | The page accurately states the policy, the `lib.es*`/decorator boundary, and that third-party-only targets are allowed. Union/intersection certainty, receiver-versus-target classification, and unknown-symbol defaults are edge ownership mechanics; omission alone does not make those statements false. | — |
| `no-new-error` | What it does | REJECTED | “Direct `new Error(...)`” accurately describes the bare identifier spelling checked by `run`. A shadowed identifier is still syntactically direct `new Error`, while aliases and `globalThis.Error` are not; symbol-resolution detail is not promised. | — |
| `no-reexports` | What it does | CONFIRMED | “Reports exports that forward bindings from another module” materially promises semantic coverage. The implementation always handles `export ... from`, but local clauses use only a textual set of top-level ES-import names and export assignments only accept a bare identifier; `import =` and member/compound assignments can forward bindings without a report. | Replace with: “Reports `export *`, namespace exports, and named specifiers in `export { ... } from`. It also reports local named export specifiers and bare-identifier export assignments whose local text matches a top-level default, namespace, or named ES-import binding. It does not resolve symbols, collect `import =` bindings, or inspect member or compound export-assignment expressions.” |
| `no-trivial-effect-fn` | What it does | REJECTED | The page already states the material gates: variable declaration, exact one `return yield*` call, positional forwarding, and an implementation-recognized service exception. Rest forwarding and the precise `.of`/`Effect.succeed` recognition heuristics are exemption and parser details, not a false service guarantee. | — |
| `no-undefined` | What it does | REJECTED | The page accurately summarizes equality comparisons, syntactic optional/undefined parameters and type properties, listed function return annotations, and direct undefined returns. Exact node lists, unwrap forms, alias non-resolution, and shadowing/fallthrough behavior are incidental syntax and certainty boundaries; its examples remain correct. | — |
| `no-unsafe-effect-apis` | What it does | CONFIRMED | “Reports value-use identifiers” is contradicted by the identifier listener: ordinary type-reference identifiers are not excluded and can report. Only identifiers with one of the listed direct parents, including a direct `TypeQuery`, are excluded; property and element listeners can still report within a type query. | Replace with: “Reports resolved identifiers in value and ordinary type-reference positions, except identifiers whose direct parent is a supported import/export form or `TypeQuery`. It also reports property accesses and string-literal element accesses, including those inside type queries, when the resolved Effect symbol name contains `unsafe` case-insensitively.” |
| `no-value-aliases` | What it does | CONFIRMED | “Parentheses and TypeScript assertion wrappers do not hide the alias” is unqualified and false for a wrapped receiver such as `(source as T).value`. `unwrapAliasExpression` removes wrappers only around the whole initializer; `isDottedValueReference` does not unwrap receivers or accept element access. | Replace with: “Reports an identifier-named `const` whose whole initializer, after outer parentheses, assertion, `satisfies`, and non-null wrappers are removed, is a bare identifier or a non-optional dot-property chain with identifier/dot-property receivers. Element access and wrappers inside a property-chain receiver are not checked.” |
| `no-weak-map` | What it does | REJECTED | The unresolved-identifier case is an uncertainty default. The page correctly covers built-in/declaration-file references and the first-party-source exemption; it need not catalog unresolved malformed references to remain true. | — |
| `observable-worker-failure` | What it does | REJECTED | Effect recognition by declaration path and logging recognition by a finite name list are certainty/list implementation details. The page’s ordinary guarantee—unrelated symbols do not match, while the Effect fixture does—and both example verdicts are supported; a contrived fallback path does not materially falsify it. | — |
| `parameter-bag` | What it does | REJECTED | The page and fixtures establish the important shape: named declarations with explicit named parameter annotations and a positional object-literal argument, while anonymous default and inferred cases are allowed. Overload selection, terminal type-name extraction, and exact unwrap forms are incidental resolution/parser limits. | — |
| `pass-through-conversion` | What it does | REJECTED | The page correctly captures the tested contract: one explicit named source and return type, equal nonempty shapes, and direct copy/spread output. Identifier-only names, terminal qualified-name comparison, and `TypeToString` equality are incidental parser/rendering details rather than a wrong documented verdict. | — |
| `prefer-composed-callbacks` | What it does | REJECTED | The direct-parent requirement and wrapper bypass are incidental listener/parser details. The documented one-parameter expression-bodied callback, recursive call-argument use, and direct one-argument-forward exemption match the implementation and fixtures. | — |
| `prefer-conditional-return` | What it does | REJECTED | Direct AST-block membership and Go byte length are incidental listener/encoding limits. The page already states the material branch/next-return form and the exclusions for multiline, over-100, `yield`, and conditional values; the documented examples are correct. | — |
| `prefer-curried-data-last-functions` | What it does | CONFIRMED | “Only references are callback arguments” claims that the receiving parameter is a callback. `hasOnlyContextualUse` checks only that each reference is a direct argument to a call whose resolved signature declaration is in a declaration/default-library file; it never checks that parameter’s type, so an `unknown` parameter also exempts the function. | Replace the exception with: “except contextually typed arrows/function expressions and named functions with at least one reference, where every reference is a direct call argument and that call’s resolved signature is declared in a `.d.ts` or default-library file. The receiving parameter is not checked to be callable.” |
| `prefer-effect-array-append-all` | What it does | CONFIRMED | “A non-empty branch” is false. `isNonEmptyArrayBranch` accepts every non-array-literal expression, including an array identifier whose runtime value can be empty; it establishes only that the arm is not syntactically `[]`. | Replace with: “Reports an array spread whose conditional expression has one arm that is exactly an empty array literal and another arm that is not an empty array literal, after unwrapping parentheses.” |
| `prefer-effect-array-append-all` | When to use it | CONFIRMED | “An array is appended only when a condition is true” is unsupported: the implementation reports either arm order, including `condition ? [] : values`, and does not prove that `values` contains elements. | Replace with: “Use it for a conditional array spread whose one arm is exactly `[]` and whose other arm is not an empty array literal. Either arm may be selected when the written condition is true.” |
| `prefer-effect-function-constant` | What it does | CONFIRMED | The page says it reports a zero-argument arrow/function expression that only returns a primitive or earlier stable `const`. `rule.go` deliberately skips `async`, generator, and generic functions, accepts only concise arrows or a one-statement `return` block, and recognizes only a finite literal set or a tightly constrained earlier `const`. This is a material overstatement, especially for async/generator contracts. | Replace the opening with: “Reports a synchronous, non-generator, non-generic zero-argument arrow or function expression with a concise expression body or exactly one `return`, when the returned value unwraps to a string/template/numeric/bigint/boolean/null literal or to an identifier for an earlier single-declaration same-file `const`.” |
| `prefer-effect-function-constant` | When to use it | CONFIRMED | “Use it when a zero-argument function always returns the same stable value” includes async, generator, and generic thunks that the rule allows. Replacing those with `Function.constant` can change the callable contract, so this is unsupported advice rather than a harmless omitted gate. | Replace it with: “Use it for the synchronous, non-generator, non-generic constant-thunk shapes reported by this rule.” |
| `prefer-effect-schema-constructor` | What it does | REJECTED | The page’s enforced verdict—non-empty raw function-local/returned objects report—remains true. `tagValue` recognizing only identifier-named `_tag` property assignments changes only whether the diagnostic names the variant; quoted or shorthand keys are an incidental message-format/parser boundary, not a material behavior claim. | — |
| `prefer-effect-schema-guard` | What it does | REJECTED | The listener starts at every `if` condition and finds literal-key `in` checks throughout ordinary expression structure. Not entering an embedded arrow block or object-property container is an incidental traversal edge; demanding “expression-classified children” would add implementation detail without materially correcting the documented rule. | — |
| `prefer-effect-schema-record` | What it does | REJECTED | “Tuple type alias” reasonably describes an alias whose target is a tuple, which `tupleType` recognizes through parentheses and `readonly`. A union, intersection, conditional, or object that merely contains a tuple is not itself a tuple type alias. The finding asks for nested-type traversal detail, not a correction to a false claim. | — |
| `prefer-eta-reduction` | What it does | REJECTED | The documented shape is accurate: one required identifier parameter, one syntactic use as the sole argument through a call tower, with checker-identified dot-method exclusion. Same-spelling counts and the lack of signature-arity proof are implementation limits; collisions mainly add edge false negatives and do not make the stated normal shape or examples wrong. | — |
| `prefer-function-composition` | What it does | REJECTED | The page already defines the three narrow AST shapes. Immediate-kind treatment of a parenthesized/asserted function initializer and textual identity checks are incidental wrapper/binding details. They do not materially contradict the ordinary block, Effect pipeline, or adapter contract shown on the page. | — |
| `prefer-function-flip` | What it does | CONFIRMED | The explicit statement “method calls are allowed” is materially false: `rule.go` exempts only an unwrapped dot-property inner callee, so `obj["method"](x)(fixed)` can report. The implementation also permits any non-dot-property inner callee, not just the displayed bare `f` shape; reporting a bracket method can make flip advice unsafe because of receiver binding. | Replace the first paragraph with: “Reports expression-bodied unary arrows whose outer call has one fixed argument and whose inner call passes the parameter as its sole argument, when the inner callee is not a dot-property access. The parameter must be plain, required, and the only same-spelled identifier use; the fixed argument must not use that spelling. Direct partial application and dot-property callees are allowed; element-access calls can report.” |
| `prefer-hash-map` | What it does | REJECTED | The documented exemption’s material contract is correct: a built-in `Map` construction passed directly or via its identifier variable to a resolved call declared outside the current file is allowed, including first-party files. Parenthesized/element-access callees, a wrapped initializer, and the exact allowed argument wrappers are incidental syntax limits whose omission does not make that contract materially false. | — |
| `prefer-hash-set` | What it does | REJECTED | The documented exemption’s material contract is correct: a built-in `Set` construction passed directly or via its identifier variable to a resolved call declared outside the current file is allowed, including first-party files. Parenthesized/element-access callees, a wrapped initializer, and the exact allowed argument wrappers are incidental syntax limits whose omission does not make that contract materially false. | — |
| `prefer-inferred-types` | What it does | CONFIRMED | The page explicitly requires only “at least one explicitly annotated parameter,” but `checkContextual` returns if any parameter has no type node. A normal multi-parameter arrow with one annotated and one inferred parameter therefore meets the written gate but is not reported. It also skips the arrow when an earlier call argument is `[]`. | Replace the contextual-arrow sentence with: “A non-variable contextual arrow reports only when no earlier argument in the same call is `[]`, there is exactly one contextual signature, every parameter is explicitly annotated and its checker-rendered type text matches the contextual parameter type, and any return annotation is equivalent to the inferred single result.” |
| `prefer-inferred-types` | When to use it | CONFIRMED | This section repeats the materially false “at least one” gate. The implementation requires every parameter to be annotated and skips contextual arrows preceded by an empty-array call argument. | Replace its final sentence with: “A non-variable contextual arrow is checked only when no earlier argument in the same call is `[]`, there is exactly one contextual signature, every parameter is explicitly annotated and its checker-rendered type text matches the contextual parameter type, and any return annotation is equivalent to the inferred single result.” |
| `prefer-layer-unwrap` | What it does | REJECTED | The page states the intended bridge and matching-tag contract, and the implementation enforces member declarations from Effect package paths plus exact resolved tag-symbol identity. Receiver-name and `pipe` spelling checks and `TypeToString` containment are certainty/alias implementation details; the finding does not show a normal documented example or guarantee with the wrong verdict. | — |
| `prefer-layer-unwrap` | When to use it | REJECTED | “Use it with Effect Layers” and “a matching tag” match the tested and implemented contract. The reported `effect`, `flatMap`, and `get` members must resolve to declarations in Effect package paths. The unresolved pipe spelling and type-text certainty gate do not materially falsify the advice that this is an Effect-API rule. | — |
| `prefer-option-match` | What it does | REJECTED | The page accurately describes the exact textual shape the implementation checks: `Option.isSome`/`Option.isNone`, an identifier input, and a `.value` read in the selected branch. Symbol resolution, shadowing, wrapper, and recursive-search details are alias/traversal limits; omitting them does not make the existing syntactic claim or example verdict wrong. | — |
| `prefer-option-match` | When to use it | REJECTED | “Other guards and non-identifier inputs are not reported” is true. Parentheses, renamed APIs, element access, and shadowed same-text identifiers are incidental syntactic/alias boundaries. The finding asks for exhaustive false-positive/false-negative detail rather than correcting unsupported usage advice. | — |
| `prefer-result-concept-names` | What it does | REJECTED | The page already states the listener forms, the exactly-one-property rule, return-first search, and whole-body fallback. The finite word vocabulary, receiver eligibility, `.map(` heuristic, plural handling, nested traversal, and exception are internal selection details; their omission does not make the high-level claim or examples false. | — |
| `prefer-result-concept-names` | When to use it | REJECTED | The advice is deliberately scoped to “the single property that the rule finds” and explicitly warns that fallback evidence may not be returned. It does not promise semantic property discovery. The requested receiver and traversal limits are exhaustive implementation detail. | — |
| `prefer-schema-tagged-struct` | What it does | REJECTED | The recognizer requires a `TaggedClass` member symbol declared under Effect package/source paths, and the page already labels the portability decision a text heuristic. Failure to separately verify the receiver spelling `Data` is an incidental alias/member-resolution detail, not a material contradiction of the intended Effect `Data.TaggedClass` contract. | — |
| `prefer-specific-operation-names` | What it does | REJECTED | “One clear role” is a concise description of the implementation’s finite evidence map after precedence reduces it to exactly one role. The vocabulary, whole-body walk, exemptions, and substring replacement are internal heuristics; omission does not make the named vague operations or shown decode verdict false. | — |
| `prefer-specific-operation-names` | When to use it | REJECTED | The section correctly names the visited callable forms and says bodies without a unique detected role are allowed. Handler/event exemptions only add allowed cases; the page does not claim that unique-role evidence is the sole condition for every report. The finding seeks an exhaustive exemption list. | — |
| `process-environment` | What it does | CONFIRMED | “An outermost property or string-element access chain” is materially narrower than enforcement. After the `process.env`/`process["env"]` root, any element access is accepted, so `process.env[key]` reports. Also, `isOutermost` does not skip non-null wrappers, so `process.env!.KEY` can produce both inner and outer reports, contradicting “an outermost” access. | Replace it with: “In a production path, reports a property or element-access chain rooted at dot `process.env` or string-element `process["env"]`, including assignment targets. Later element keys may be dynamic. Parentheses, `as`, and `satisfies` are skipped for outermost detection, but a non-null wrapper can cause both an inner and outer access to report.” |
| `raw-fetch-abort-signal` | What it does | CONFIRMED | The phrase “containing raw fetch syntax” materially understates `hasFetch`: its whole-call regex also matches `client.fetch(`, `$fetch(`, and fetch-like text in comments or strings. The parameter regex likewise scans whole-call text and can capture identifier-shaped text that is not the callback parameter. Thus the page misstates what calls the rule enforces. | Replace the opening with: “For an exact textual `Effect.tryPromise` or `tryPromise` call, scans the entire call text with `(?:\bfetch\|(?:globalThis\|window\|self)\.fetch)\s*\(`. This is not AST fetch recognition: it can match custom/qualified calls such as `client.fetch` or `$fetch` and text in comments or strings. Parameter extraction is also a whole-call regex and need not identify the callback parameter.” Keep the existing acceptance-regex caveat. |
| `raw-fetch-abort-signal` | When to use it | CONFIRMED | Calling this a prompt around “raw `fetch`” promises a narrower target than the regex implements. Common unrelated textual forms such as `client.fetch` and `$fetch` can trigger, while unrelated brace-delimited signal text can satisfy the rule. | Replace it with: “Use this broad whole-call text check as a cancellation prompt around fetch-like text in `tryPromise`. It does not prove that the call is raw `fetch`, that the extracted name is the callback signal, or that an accepted `signal` occurrence is `fetch`’s `init.signal`.” |
| `raw-fetch-outside-adapter` | What it does | CONFIRMED | “Outside an adapter or `Effect.tryPromise`” is materially over-broad. `insideTryPromise` stops at a non-immediate function-like ancestor, so the shown expression callback is exempt but the normal block callback `Effect.tryPromise(() => { fetch("/ok") })` reports. Only four exact unresolved fetch callee texts and exact lowercase adapter path segments are recognized. | Replace it with: “Reports calls whose unresolved callee text is exactly `fetch`, `globalThis.fetch`, `window.fetch`, or `self.fetch`, unless the file has an exact `adapter`/`adapters` path segment or an exact textual `Effect.tryPromise`/`tryPromise` call ancestor is reached before crossing a non-immediate function-like ancestor. An expression-body callback can be exempt while a block-body callback reports.” |
| `raw-fetch-outside-adapter` | When to use it | REJECTED | “Use it to keep raw network access at explicit boundaries” is valid advice, and files in exact `adapter`/`adapters` path segments are allowed as stated. The omitted shape-sensitive `tryPromise` exemption is an implementation limit from the What-it-does contract; its omission here does not create a separate false usage guarantee. | — |
| `redundant-alias` | What it does | REJECTED | The page correctly states the two declaration kinds, the single-empty-interface shape, leading reference-token alias scan, optional utility wrapper, and unchecked trailing syntax. The permissive optional-`<` regex changes how names like `PartialCustomer` are split in the message, and self-extension is suppressed by the shared same-name check; these are edge parser/message exemptions, not a material false rule claim. | — |
| `require-because-in-comments` | What it does | REJECTED | Actual line and block comments without standalone `because` are reported as stated. Treating comment-looking delimiters inside rare regexp literals (for example `/[//]/`) as comments is a custom-scanner edge false positive. The page does not promise a trivia-parser implementation, so documenting scanner/string/template/regexp mechanics is not required for source fidelity. | — |
| `require-callable-role-name-consistency` | What it does | REJECTED | The page accurately limits the rule to the three identifier-named callable forms, the two-word/role-noun gate, explicit return text, and body checks for factory/accessor. The fixture proves the stated `activePredicate` verdict. The finding asks for each role's internal text heuristic and weak/asymmetric edge behavior; that omitted detail does not make “against their signature or body shape” false. | — |
| `require-command-name-consistency` | What it does | REJECTED | The page's condition that command names require explicit return text containing `void` or `effect` is true as a required condition, and the fixture proves both stated verdicts. Classifier priority for the edge annotation `() => void`, the inverse finite lists, and the unused body are extra implementation details; the page does not promise that every annotation merely containing `void` passes. | — |
| `require-construction-name-consistency` | What it does | REJECTED | The source and fixture support the stated `user(): User` report, the four construction verbs, the variant allowance, and the lookup rejection. Listener gates, raw construction substrings, explicit-return concept extraction, and lack of semantic freshness analysis narrow detection but do not contradict any example or guarantee stated on the page. | — |
| `require-lookup-totality-name-consistency` | What it does | REJECTED | The page expressly speaks about annotated returns, and the fixture establishes the documented `User` versus `User \| undefined` boundary. Substring classification order and unsupported `Option`/`Result` spellings are unclaimed parser/alias limits, not a wrong documented verdict. | — |
| `require-predicate-name-consistency` | What it does | REJECTED | Production and the fixture support the documented identifier-callable scope and the `isUser(): User`/`isReady(): boolean` verdicts. `=>` classifier priority, exact predicate/incompatible word lists, and omitted words from diagnostic help are incidental finite-list and edge details; “requires ... text containing boolean or a type-predicate form” states a necessary condition, not that every larger annotation containing `boolean` is accepted. | — |
| `require-result-cardinality-name-consistency` | What it does | REJECTED | The concrete claim that arrays, sets, maps, and records need a plural result noun (apart from neutral nouns) matches production, and the fixture proves `getUser(): ReadonlyArray<User>` reports while `getUsers` passes. The asymmetric reverse check for plural names returning one object is an omitted limit, not a contradiction of that documented claim. | — |
| `schema-error-class` | What it does | REJECTED | The documented positive allowance for `Schema.TaggedErrorClass` and the exact `_tag`/`Error` example both match the fixture and production. Regex provenance, aliases, and accidental `Schema` text only describe permissive parser edge cases; the page does not claim symbol resolution or that those are the only possible allowances. | — |
| `schema-optional-key` | What it does | REJECTED | The page already states the material behavior: matching is file-wide by property name and does not prove interface/schema correspondence. Its import and call spelling works exactly as shown in both fixtures. Textual import resolution, shadowing, and unsupported import forms are incidental parser limits, not a false existing statement. | — |
| `scoped-background-work` | What it does | REJECTED | The page says “recognized scope owner,” lists the checked background APIs, and gives the exact tested `forkDaemon`/`forkScoped` boundary, which the source and fixtures confirm. Exact owner method lists, syntactic ancestry, textual imports, and possible duplicate reports are omitted exemption/traversal/parser mechanics rather than a wrong documented verdict. | — |
| `scoped-client-cache` | What it does | REJECTED | The finite checked `Effect`, `Layer`, and `Cache` methods and the whole-cache-call nesting boundary are already documented and match the fixture. Regex import parsing and shadowing are incidental resolution limits; they do not invalidate the shown imported `Cache.make`/`Effect.provide` behavior. | — |
| `service-method-effect-fn` | What it does | CONFIRMED | “Reports public Effect operations that are not wrapped with a named `Effect.fn`” is materially false. In a raw-text `Context.Service` class, production recursively reports methods/properties without any accessibility gate, so private or nested declarations can report. Conversely, any descendant named `Effect.fn` with a string first argument exempts the entire value even when it does not wrap the operation. | Replace the opening with: “Reports qualifying syntactically exported variables/functions, plus recursively found methods and object properties in any class whose source contains `Context.Service`; class members are checked without an accessibility filter. A value is allowed when its subtree contains a recognized `Effect.fn` call whose first argument is a string literal, or any recognized `Effect.gen` call.” |
| `speculative-export` | What it does | REJECTED | The page deliberately says the declaration's **name does not appear** in another first-party file, not that the rule resolves a consumer symbol. That matches production's cross-file raw-word occurrence test and the consumer fixture. Comment/string suppression, tokenization, and export-list listeners are extra parser/listener details; the documented example verdicts remain right. | — |
| `stream-pagination` | What it does | REJECTED | The page names exactly the three loop kinds and the supported accumulation operations, and the fixture proves the manual-loop and `Stream.paginate` verdicts. Recursive subtree correlation, finite token regexes, call-access forms, and import resolution are omitted traversal/parser details; “combines” is consistent with the implementation's subtree-presence test. | — |
| `test-clock-for-time` | What it does | REJECTED | The documented `@effect/vitest` test, time-call, and file-wide `TestClock` behavior matches both fixtures. Exact method lists, lexical ancestry, helper-call limits, textual imports, and the `TestClock` regex are listener/parser/exemption details whose omission does not make the shown claim or verdict false. | — |
| `test-sleeps` | What it does | REJECTED | Production and fixtures confirm that a recognized `Effect.sleep` lexically inside an imported `@effect/vitest` test reports and other Effect calls do not. Exact `it` spellings, no call-graph traversal, shadowing, and import parsing are incidental entry/parser limits, not contradictory page advice or examples. | — |
| `typed-boundary-error` | What it does | REJECTED | The page's four API names and shown `new Error` versus `new LoadError` verdicts match source and fixtures. Handler-argument selection, nested traversal, “any throw,” terminal-name matching, and textual import identity are unclaimed traversal/parser limits; “throw or construct `Error`” does not promise built-in symbol resolution. | — |
| `typed-error-recovery` | What it does | REJECTED | The checker type rendering and fixture establish the documented `Failure` report and exact `never` allowance. Data-first/immediate-pipe selection, regex certainty, nested rendering, and unsupported/shadowed forms are uncertainty and parser limits; the page does not enumerate supported call shapes or promise diagnostics when the implementation cannot establish the channel. | — |
| `unbounded-stream-buffer` | What it does | REJECTED | The page already states the central syntactic boundary: an object-literal `capacity` with exact string `"unbounded"`; the numeric and string fixtures match. Direct-property forms, wrappers, constants/spreads, all-argument traversal, and textual import resolution are incidental syntax/parser details, not a false claim. | — |
| `unbounded-stream-collect` | What it does | REJECTED | For every recognized Effect `Stream.runCollect` call, production reports without a boundedness or context exemption, exactly as the page states and fixture shows. Unsupported imports, re-exports, and shadowing only concern textual API recognition, an incidental parser/alias limit that does not require cataloging here. | — |
| `unused-field` | What it does | CONFIRMED | “Reports such a field when no independent read exists” and “constructed but never independently read” materially misstate the enforced boundary. `collectReads` counts every non-declaration identifier carrying the field symbol, including assignment targets, so a write can suppress the report even though no read exists. The special same-name forwarding form is the only such symbol use excluded. | Replace the read claim with: “Reports a field when its checker symbol has no non-declaration identifier use; writes count as uses. The exact forwarding shape `{ field: value.field }` does not count, while a textual `Struct.get("field")` call counts that field name program-wide.” |

## Summary

- Failed judgments adjudicated: **91**.
- **CONFIRMED: 25** across **21 pages**.
- **REJECTED: 66**.
- Confirmed axes: **20 `What it does`** and **5 `When to use it`**.
- No Conformant or Non-conformant finding was reported in the third rerun.

### Confirmed pages and axes

- `bounded-retry-schedule` — `What it does`
- `duplicate-shape` — `What it does`
- `effect-fn-name` — `What it does`
- `effect-test-style` — `What it does`
- `http-status-decode-order` — `What it does`
- `no-blank-lines-between-single-line-declarations` — `What it does`
- `no-duplicate-function-names` — `When to use it`
- `no-immediate-effect-sync` — `What it does`
- `no-reexports` — `What it does`
- `no-unsafe-effect-apis` — `What it does`
- `no-value-aliases` — `What it does`
- `prefer-curried-data-last-functions` — `What it does`
- `prefer-effect-array-append-all` — `What it does`, `When to use it`
- `prefer-effect-function-constant` — `What it does`, `When to use it`
- `prefer-function-flip` — `What it does`
- `prefer-inferred-types` — `What it does`, `When to use it`
- `process-environment` — `What it does`
- `raw-fetch-abort-signal` — `What it does`, `When to use it`
- `raw-fetch-outside-adapter` — `What it does`
- `service-method-effect-fn` — `What it does`
- `unused-field` — `What it does`

### Page-group impact

| Page group | Judgments | Confirmed | Rejected | Confirmed pages |
| --- | ---: | ---: | ---: | --- |
| `DOC-01` | 11 | 4 | 7 | `bounded-retry-schedule`, `duplicate-shape`, `effect-fn-name`, `effect-test-style` |
| `DOC-02` | 5 | 2 | 3 | `http-status-decode-order`, `no-blank-lines-between-single-line-declarations` |
| `DOC-03` | 8 | 2 | 6 | `no-duplicate-function-names`, `no-immediate-effect-sync` |
| `DOC-04` | 4 | 0 | 4 | — |
| `DOC-05` | 5 | 2 | 3 | `no-reexports`, `no-unsafe-effect-apis` |
| `DOC-06` | 8 | 2 | 6 | `no-value-aliases`, `prefer-curried-data-last-functions` |
| `DOC-07` | 5 | 4 | 1 | `prefer-effect-array-append-all`, `prefer-effect-function-constant` |
| `DOC-08` | 7 | 1 | 6 | `prefer-function-flip` |
| `DOC-09` | 16 | 6 | 10 | `prefer-inferred-types`, `process-environment`, `raw-fetch-abort-signal`, `raw-fetch-outside-adapter` |
| `DOC-10` | 8 | 0 | 8 | — |
| `DOC-11` | 6 | 1 | 5 | `service-method-effect-fn` |
| `DOC-12` | 8 | 1 | 7 | `unused-field` |

All 12 page groups had at least one third-rerun finding. Confirmed corrections affect 10 groups. `DOC-04` and `DOC-10` have only rejected findings.

## Blockers

- None. All 91 findings had readable pages, production files, tests, and fixtures.
- This report is read-only adjudication evidence. It does not apply the confirmed corrections.
