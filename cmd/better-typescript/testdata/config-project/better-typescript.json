{
  "commands": [
    {
      "type": "add_exclusions",
      "files": "src/**/*.ts",
      "rules": ["no-throw"]
    },
    {
      "type": "add_inclusions",
      "files": "src/main.ts",
      "rules": "no-throw"
    },
    {
      "type": "add_exclusions",
      "files": "src/nested/disabled.ts",
      "rules": ["no-error-type"]
    }
  ]
}
