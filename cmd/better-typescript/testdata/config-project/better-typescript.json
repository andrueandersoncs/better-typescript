{
  "commands": [
    {
      "type": "add_inclusions",
      "files": "src/**/*.ts",
      "rules": ["no-error-type", "no-throw"]
    },
    {
      "type": "add_exclusions",
      "files": "src/nested/**/*.ts",
      "rules": "no-throw"
    },
    {
      "type": "add_exclusions",
      "files": "src/nested/disabled.ts",
      "rules": ["no-error-type"]
    }
  ]
}
