export declare const EffectTypeId: unique symbol

export interface Effect<A, E = never, R = never> {
  readonly [EffectTypeId]: {
    readonly _A: () => A
    readonly _E: () => E
    readonly _R: () => R
  }
}

export declare function tryPromise<A, E = unknown>(
  evaluate: (signal: AbortSignal) => PromiseLike<A>,
): Effect<A, E>

export declare function tryPromise<A, E>(options: {
  readonly try: (signal: AbortSignal) => PromiseLike<A>
  readonly catch: (cause: unknown) => E
}): Effect<A, E>
