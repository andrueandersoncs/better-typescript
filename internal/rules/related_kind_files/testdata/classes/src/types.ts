export interface ErrorData { readonly error: string }
export interface ServiceData { readonly service: string }
export interface SchemaData { readonly schema: string }
export interface ConstructorData { readonly constructor: string }
