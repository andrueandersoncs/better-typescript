// SemanticRole is shared because naming rules must use one callable behavior vocabulary.
export type SemanticRole =
  "aggregation" | "command" | "construction" | "conversion" | "lookup" | "projection"
