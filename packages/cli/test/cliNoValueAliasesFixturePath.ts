import * as path from "node:path"
import { repoRoot } from "./cliRepoRoot.js"

export const noValueAliasesFixturePath = path.join(
  repoRoot,
  "packages/rules/src/rules/no-value-aliases/fixtures/rule"
)
