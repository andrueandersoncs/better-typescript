// RoleWord is a local syntax union because matchers need one narrowed node shape.
export type RoleWord =
  | "accessor"
  | "callback"
  | "comparator"
  | "factory"
  | "function"
  | "handler"
  | "mapper"
  | "predicate"
  | "reducer"
